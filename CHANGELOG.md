# GRAPHIDEA 변경 기록

> 프로젝트명: **GRAPHIDEA** (부제 "Idea Connect"). 이전 명칭 STACKER에서 리브랜딩(2026-07-26).

> 단일 파일 앱(`index.html`). 순수 HTML/CSS/JS + Supabase(Auth/Postgres/Realtime) + Vercel 자동 배포.
> GitHub: SpecialBee/NoteApp · 배포: https://note-app-gamma-wheat.vercel.app

---

## 2026-08-26 — 순서 목록 번호가 전부 "1."로 나오던 문제

`1.` `2.` `3.` `4.`로 적은 목록이 미리보기에서 전부 **"1."**로 렌더되던 버그 수정.

### 원인
`buildListHtml`이 항목을 파싱할 때 `ordered: m[2] !== '-'`로 **"순서 목록이다"라는 사실만 남기고 사용자가 적은 숫자는 버리고** 있었고, `renderList`는 언제나 맨 `<ol>`을 열었다. HTML `<ol>`은 `start`가 없으면 항상 1부터 세므로, **목록이 한 번 끊겨 새 `<ol>`이 열릴 때마다 번호가 리셋**됐다. 항목 사이의 빈 줄이나 중간에 낀 `- ` 불릿이 전부 목록을 끊기 때문에(리스트 정규식이 연속된 목록 줄만 한 블록으로 묶음), 실제 사용 패턴에서 거의 항상 재현되는 상태였다.

### 처리
파싱 때 `num: parseInt(m[2], 10)`으로 타이핑된 숫자를 보존하고, `renderList`가 run의 첫 항목 번호가 1이 아니면 `<ol start="N">`으로 연다. 표준 마크다운이 순서 목록의 첫 숫자를 다루는 방식과 동일.

### 검증
`buildListHtml`을 추출해 케이스별 출력 확인 + Chromium 실제 렌더 확인.

| 입력 | 결과 |
|---|---|
| 빈 줄로 끊긴 `1.`/`2.`/`3.`/`4.` (제보된 케이스) | 1, 2, 3, 4 |
| 연속된 `1.`/`2.`/`3.` | 1, 2, 3 |
| 전부 `1.`로 적는 관용 표기 | 1, 2, 3 (브라우저 자동 번호) |
| `3.`부터 시작 | 3, 4 |
| 들여쓴 하위 항목 | 중첩 유지 |

### 참고
들여쓰기 없이 번호 항목 바로 뒤에 붙은 `- ` 불릿은 하위 항목이 아니라 **별개 목록**으로 렌더된다(표준 마크다운 동작). 하위로 넣으려면 공백 2칸 들여쓰기 필요.

---

## 2026-08-21 (5) — 미리보기 표가 패널을 넘치던 문제

미리보기/읽기모드에서 표가 패널 너비를 무시하고 늘어나 가로 스크롤이 생기던 것을 **패널에 맞춰 줄어들고 셀 글자가 접히도록** 수정.

### 원인
표 셀에 `white-space:nowrap`, 표 자체에 `width:max-content`가 걸려 있어 구조적으로 "내용만큼 늘어나고 넘치면 스크롤"이었다.

### 처리 (`.preview table` / `th,td`)
- `width:100%` (was `max-content`) — 표가 컨테이너를 넘지 않음
- `white-space:normal` (was `nowrap`) — 셀 글자가 여러 줄로 접힘
- **`overflow-wrap:anywhere`** — `break-word`가 아니라 `anywhere`인 게 중요. `anywhere`만 셀의 **min-content 너비까지 낮춰줘서** 열이 실제로 좁아질 수 있다(안 끊기는 긴 URL도 이걸로 처리됨).
- `min-width:64px` — 열이 아주 많으면 읽을 수 없는 폭으로 찌그러지느니 `.tbl-wrap`의 가로 스크롤로 넘기는 게 낫다고 판단.

### 검증 (Chromium 실측)
패널 380px에서 긴 셀 내용 기준 표 폭 **834px → 380px**, 가로 스크롤 사라짐. 패널을 996→256px로 좁히며 표 폭이 항상 패널과 일치하고 행 높이가 37→177px로 증가(줄바꿈이 실제로 일어남)하는 것 확인. 열 12개일 때는 의도대로 표만 스크롤. 이미지는 이미 `.md-img{max-width:100%}`로 제약돼 있어 손댈 것 없었음.

---

## 2026-08-21 (4) — 미니 그래프 삭제 + "연결 지도"로 명칭 통일

### 우측 패널 미니 그래프 삭제
카드 편집 시 우측 패널 아래쪽에 있던 240×150 SVG 미니 그래프를 제거했다. 전날 붙인 **연결 지도의 노드 연결관계 패널**이 "이 카드는 뭐랑 이어져 있지?"라는 같은 질문에 방향 구분·walk까지 얹어 훨씬 잘 답하게 되면서 역할이 완전히 겹쳤고, 우측 패널에서는 자리만 차지하고 있었다.
- 제거 대상: `#graphMini` 마크업, `drawGraph()`/`miniSim`/`stopMiniSim`/`reheatMiniSim`/미니 드래그 핸들러 일체(약 160줄), `.graphmini` CSS 전부.
- 공유 헬퍼 `startIdleFloat()`은 대시보드 히어로 그래프가 계속 쓰므로 남김(관련 주석만 정정).
- 우측 패널 구성: 속성 / 역링크 / 캔버스 보드 / 태그 / **목차**.

### "전역 그래프" → "연결 지도" 명칭 통일
미니 그래프가 쓰던 이름이 비면서, 그동안 "전역 그래프"·"전체 그래프"로 섞여 불리던 화면을 **연결 지도** 하나로 통일.
- 사이드바 버튼 `🕸 전역 그래프` → `🕸 연결 지도`, 화면 헤더 `전역 연결 그래프` → `연결 지도`, 대시보드 `전체 그래프 보기 →` → `연결 지도 열기 →`, 히어로 더보기 `외 N개 — 전체 그래프에서 보기` → `… 연결 지도에서 보기`.
- 사용법 가이드도 갱신(미니 그래프 언급 삭제, 연결 지도 항목에 클릭=선택·더블클릭=열기 설명 추가).
- **코드 식별자는 그대로**(`globalGraphBtn`, `renderGlobalGraph`, `drawGlobalGraph` …). 전부 개명하면 diff만 커지고 히스토리 추적이 어려워질 뿐이라 UI 문구만 바꿨다.

### 참고
`www/index.html`·`www/style.css` + 문서. DB 스키마 변경 없음. 문법 검증(`new Function()`) 및 삭제 심볼 잔존 검사 통과.

---

## 2026-08-21 (3) — 제목 조회 색인 (O(n²) 제거)

노트가 1,000개가 되면서, **"이 제목의 노트가 뭐지?"를 매번 전체 목록을 훑어서 답하던 구조**가 한계에 닿았다. 같은 뿌리의 문제가 6곳에 퍼져 있었고, 전부 뜨거운 경로(타이핑·자동저장·그래프 열기)에 있었다.

### 문제
제목으로 노트를 찾는 색인이 없어서, 모든 조회가 `index.find(m => m.title.toLowerCase() === t.toLowerCase())` 형태의 선형 스캔이었다. 비교마다 `toLowerCase()`로 버리는 문자열을 새로 만드는 것도 컸다.
- `renderGlobalGraph` 엣지 구축: `노트 n개 × 링크 3개 × 다시 n개 스캔` — 그래프 열기·리사이즈·토글마다 반복
- `renderMarkdown` 위키링크 판정: **키 입력마다** 링크 개수만큼 전체 스캔. 특히 **깨진 링크는 매번 끝까지** 훑음(조기 종료가 없음)
- `renderRightPane` 역링크 + 미니 그래프: 자동저장(0.6초)마다 전체 스캔 ×2

### 처리
- **`rebuildNoteIndexes()`**: 기존 `computeAllTags()`가 이미 "index가 바뀌었다"는 훅으로 15곳에서 불리고 있어 여기에 통합(이름이 하는 일과 맞지 않게 되어 함께 개명). 한 번의 순회로 태그 집합 + 아래 두 색인을 같이 만든다.
  - `noteByTitle`: 소문자 제목 → 노트. 중복 제목은 **먼저 나온 것이 이김** — 기존 `Array.find()`와 동일.
  - `notesLinkingTo`: 소문자 제목 → 그 제목을 링크하는 노트 배열(역링크). 한 노트가 같은 제목을 두 번 링크해도 **한 번만** 담기도록 처리(기존 `filter(...some(...))`와 동일).
- 조회 6곳을 `findNoteByTitle()` / `backlinksOf()`로 교체.
- **검색 입력 디바운스(120ms)**: 한 글자마다 1,000개 필터링 + 사이드바 DOM 전체 재구성이 돌던 것을 입력이 멎은 뒤 1회로.

### 검증
- **차등 테스트**: 신규 색인과 기존 선형 스캔 구현을 같은 데이터(중복 제목/대소문자 불일치/자기 링크/빈 제목/`links: null`/같은 제목 중복 링크)에 돌려 결과가 완전히 일치하는지 비교. **중복 링크 시 역링크가 두 번 나오는 버그를 이 테스트가 잡아내 수정**했고, 수정 후 전 케이스 일치.
- **벤치마크**(노트 1,000개, 노트당 링크 3개):

  | 항목 | 이전 | 이후 |
  |---|---|---|
  | 그래프 엣지 구축 1회 | 106 ms | **0.7 ms** |
  | 미리보기 위키링크 10개 × 100타(깨진 링크) | 87 ms | **0.9 ms** |
  | 역링크 조회 × 100회(자동저장) | 22.6 ms | **0.2 ms** |
  | 색인 재구축(index 변경 시) | — | 0.5 ms |

  그래프 열 때마다 약 0.1초씩 메인 스레드가 멈추던 것이 사실상 사라짐.

### 참고
- `www/index.html` 한 파일. DB 스키마 변경 없음. 문법 검증(`new Function()`) 통과.
- 다음 후보: 사이드바 카드 목록 가상화(1,000개면 DOM 노드도 1,000개) — 위 변경만으로 충분한지 먼저 체감 후 판단.

---

## 2026-08-21 (2) — 1,000행 상한 대응

노트 수가 1,000개에 근접하면서, **미뤄뒀던 PostgREST 응답 상한이 실제 데이터 손실 위험**이 됐다.

### 왜 급했나
Supabase(PostgREST)는 한 요청에 최대 1,000행만 돌려주는데, **잘라내면서 오류를 내지 않는다.** 그래서 1,000개를 넘는 순간부터 초과분이 목록·검색·전역 그래프에서 조용히 사라지고, 앱은 멀쩡해 보인다. 가장 나쁜 건 **백업**이었다 — "전체 백업(JSON)"과 "마크다운 zip 내보내기"가 성공한 것처럼 파일을 내려주면서 실제로는 1,000개까지만 담고 있었다. 백업을 믿고 있는 상태에서 조용히 불완전해지는 게 단순 표시 버그보다 훨씬 위험하다고 판단해 최우선 처리.

### 처리
- **`fetchAllRows(buildQuery)` 헬퍼 추가**: `.range(from, from+999)`로 페이지를 돌며 짧은 페이지가 나올 때까지 누적. Supabase 쿼리 빌더는 한 번 await하면 재사용이 안 되므로 **빌더를 만드는 팩토리 함수**를 받는 형태. 폭주 방지용 상한(200페이지 = 20만 행)도 둠.
- **적용 대상** — 테이블 전체를 반환할 수 있는 모든 쿼리:
  - `loadAll` (전체 노트 목록), `loadTrash` (휴지통)
  - **`backupAllBtn` (전체 백업 JSON)**, **마크다운 zip 내보내기** ← 침묵 절삭이 가장 위험했던 곳
  - `loadChecklist` (할 일 목록)
  - `cascadeRename`·`renderCanvasBacklinks` — `ilike` 필터라 상한에 닿을 일이 사실상 없지만, 같은 헬퍼로 통일해 예외를 남기지 않음
- **검증**: 헬퍼를 1,000행에서 잘라내는 가짜 API에 물려 0/1/999/**1000**/**1001**/2000/2500/5432행 케이스를 실행 — 전부 전량 수집·순서 유지 확인(1,000의 배수 경계에서 한 번 더 빈 페이지를 받고 종료하는 동작 포함). 오류 전파 경로도 확인.

### 참고
- `www/index.html` 한 파일 + 문서. DB 스키마 변경 없음. 문법 검증(`new Function()`) 통과.
- 문서 정정: `PROJECT-RECORD.md`의 연결관계 패널이 "미구현"으로 남아 있던 것을 구현 완료로 갱신.

---

## 2026-08-21 작업 요약

전역 그래프의 **연결관계 탐색 패널**을 새로 붙이고, **앱 첫 로드 버벅임**을 잡았다.

### 전역 그래프 — 노드 연결관계 패널
클릭 시 곧바로 카드로 이동하던 걸, **클릭=선택 + 연결 패널**로 바꿔 그래프를 "탐색" 도구로 승격. 기획안(`CONNECTION-PANEL-PLAN.md`)의 결정 항목을 확정해 구현.
- **클릭/탭 = 선택**(sticky 하이라이트: 선택 노드에 accent 링 + 이웃만 밝게), 빈 곳 클릭 = 해제. **더블클릭 = 카드 열기**(모바일은 패널의 "카드 열기 →" 버튼).
- **패널 내용**: 타입아이콘+제목, 태그, `나가는 링크 N · 백링크 N`, 그리고 나가는 링크/백링크를 **방향 구분**해 목록화(각 항목 태그색 점). `links`가 목록 필드라 본문 로드 없이 즉시 구성. 대상 카드가 없는 위키링크(깨진 링크)는 **흐리게(클릭 불가)** 노출.
- **연결 항목 클릭 = walk**: 그래프에 있으면 선택을 그 노드로 옮기고 화면 중앙으로 부드럽게 팬(`centerGraphOnNode` rAF 트윈, 수동 팬/줌/드래그 시 취소), 없으면 그 카드를 엶.
- **패널은 오버레이 도킹**(그래프 전용)이라 토글해도 캔버스 리사이즈/재시뮬이 없음. 모바일은 하단 시트, 안드로이드 뒤로가기에 "패널 먼저 닫기" 단계 추가. `drawGlobalGraph`는 hover와 selection을 합친 focus 개념으로 dim/라벨 처리.

### 앱 첫 로드 버벅임 완화
"한 번에 많은 정보를 여는" 체감 렉의 원인을 부팅 경로에서 추적해 중복 작업을 제거:
- **`renderCardList` O(n²) → O(n)**: `buildCardEl`이 카드마다 `index.findIndex`(문서번호용)를 돌던 것을 `id→위치` Map 1회 생성으로 교체. 카드가 많을수록 극적.
- **DocumentFragment**: 카드를 라이브 `#cardList`에 N번 append하며 리플로우가 반복되던 것을 fragment에 모아 **1회 삽입**.
- **부팅 네트워크 병렬화**: 불필요한 `getUser()` 왕복 제거(이미 세션에 user 있음), `loadAll`·`loadChecklist`를 `Promise.all`로 병렬.
- **카드 리스트 이중 렌더 제거**: 부팅 시 `boot()`에서 1회 + 이어지는 `openDashboard()`에서 또 1회 = 전체 목록 DOM을 두 번 짓고 있었음. 대시보드를 여는 경로에선 중복 호출 제거.
- **⭐ boot-once 가드**: `onAuthStateChange`가 `getSession()`과 겹쳐 초기 로드에 `boot()`이 두 번 돌고, 게다가 **토큰 갱신(TOKEN_REFRESHED)·USER_UPDATED마다 전체 재로딩**이 일어날 수 있었음. `bootedUserId`로 user별 1회만 부팅하도록 가드(로그아웃 시 리셋). 실시간 동기화는 유지되므로 데이터 최신성엔 영향 없음.

### 참고
- 변경은 `www/index.html`·`www/style.css` 두 파일 + 문서. DB 스키마/인프라 변경 없음. 문법 검증(`new Function()`) 통과.
- 남은 최적화 옵션(필요 시): 카드 청크/가상화 렌더(`requestIdleCallback`), 카드 이벤트 위임.
- **안드로이드 반영 필요**: 이번 웹 변경(연결 패널 터치/뒤로가기 포함)은 `npx cap sync android` + 재빌드 후 실기기에 들어감.

---

## 2026-08-20 작업 요약

**전역 그래프의 렉을 근본 해결** — 렌더러를 SVG에서 Canvas로 전면 전환하고, 팬/줌·라벨·입체 효과까지 3단계로 다듬었다. 물리 시뮬레이션(힘 계산)은 손대지 않고 **매 프레임의 그리기 계층만** 교체한 것이 핵심. (대시보드 히어로 그래프·우측 패널 미니 그래프는 여전히 SVG — 노드 수가 적게 캡돼 있어 렉이 없음.)

### 렉의 진짜 원인
이전 세션에서 반발력 거리 컷오프·`|0` 절삭 등 **계산 쪽** 최적화를 다 했는데도 렉이 남았음. 실제 병목은 계산이 아니라 **렌더링**이었다: `step()`이 매 프레임 노드마다 `setAttribute('transform')`, 선마다 `x1/y1/x2/y2` 4회씩 SVG 속성을 갱신 → 노드 500·선 800이면 프레임당 약 3,700회, 60fps면 초당 22만 회. 각 호출이 retained SVG 씬 트리의 스타일 재계산·레이아웃을 무효화하는 게 렉의 근원. Obsidian이 그래프에 Canvas/WebGL을 쓰는 이유와 동일.

### Stage 1 — 렌더러 캔버스 전환
- `<svg id="globalGraphSvg">` → `<canvas id="globalGraphCanvas">`. 매 프레임 수천 번의 `setAttribute` 대신 **즉시(immediate) 모드로 씬 전체를 1회 repaint**(`drawGlobalGraph`) — retained DOM/레이아웃 무효화가 사라짐.
- DPI 대응 백버퍼(`sizeGraphCanvas`, css폭×devicePixelRatio)로 레티나/모바일 선명도 확보.
- 캔버스는 CSS 변수 색을 직접 못 쓰므로 프로브 요소로 `var(--…)`→rgb 해석(`resolveCssColor`/`toRgb`) + 알파 합성(`withAlpha`).
- 히트테스트를 DOM(`e.target.closest('.gnode')`)에서 **수동 최근접 탐색**(`graphClientToWorld`/`graphNodeAt`)으로 대체.
- world(viewBox)→canvas "meet" 베이스 변환(`baseScale/baseTx/baseTy`)을 `globalSim`에 저장해 기존 viewBox 팬/줌 감각을 그대로 재현. 정착 후 흔들림도 캔버스 기반(`startGlobalIdleFloat`).
- 클릭/호버/검색/팬/줌/드래그/터치·핀치 핸들러 전부 캔버스 히트테스트로 재작성. dim/검색매치/hub·pinned 라벨 게이팅 등 기존 CSS 로직을 draw 함수로 이식.

### Stage 1 후속 — 팬/줌 좌표 보정
`graphView.tx/ty`가 viewBox 단위인데 팬/줌 계산은 화면 px로 하고 있어 `baseScale`(노드 많을수록 <1)만큼 어긋났음: 끌어서 이동이 짧고, 휠 확대가 커서가 아니라 좌상단 쪽으로 쏠림. 마우스/터치 팬은 `/baseScale`, 휠 줌·터치 핀치는 커서(핀치 중점)를 viewBox 좌표로 환산해 고정점으로 쓰도록 수정.

### Stage 2 — 라벨 시각 마감
- 라벨을 노드/엣지와 분리해 **화면 좌표에서 2-패스로** 렌더 → 월드 변환에 딸려 작아지지 않고 고정 12px로 선명·가독.
- 글자 뒤 배경색(`--bg-panel`) `strokeText` 후광으로 엣지/노드 위에서도 읽힘.
- 우선순위(검색매치>호버>hub>pinned>기타) 정렬 후 겹침 사각형 컬링 → 밀집 구역 글자 뭉침 방지(매치/호버는 항상 자리 확보), 뷰포트 밖 라벨 컬링. 격렬한 정착 애니메이션 중엔 호버/매치 라벨만 그려 hot path 경량 유지.

### Stage 3 — 입체(depth) 모드 개선
- 캔버스에서 per-node 가우시안 블러는 비싸고 줌 배율에 흔들려서, **대기 원근(atmospheric perspective)**으로 전환. 먼 노드(낮은 z)일수록 색을 배경색 쪽으로 바래고(`mix(fill, bg, (1-z)*0.62)`) 작게, 가까운 노드는 채도·크기 유지.
- 노드 빌드 시 fill을 배경색과 사전 보간해 캐시 → 매 프레임 비용 사실상 0. 단순 투명도보다 "깊이"로 읽힘.

### 참고
- 변경은 `www/index.html`·`www/style.css` 두 파일뿐(죽은 `#globalGraphSvg .gnode/line` CSS 제거). DB 스키마/인프라 변경 없음. 문법 검증(`new Function()`) 통과.
- **신규 기획안**: `CONNECTION-PANEL-PLAN.md` — 노드 클릭 시 즉시 카드로 이동하는 대신, 그 노드의 나가는 링크/백링크를 오른쪽 패널에 보여주고 그래프를 walk하는 기능(미구현, 결정 대기).
- **안드로이드 반영 필요**: 이번 웹 변경(특히 캔버스 그래프 터치 팬/줌·핀치)은 `npx cap sync android` + 재빌드해야 실기기에 들어감.

---

## 2026-08-11 작업 요약

**앱의 정체성을 "개인용으로 완전히 확정"** — 출시용 트랙은 사용자가 직접 폴더를 복사해 별도로 시작하기로 하고(git 브랜치 제안은 했으나 폴더 복사를 선택), 이 저장소는 이제부터 개인 사용을 위한 고도화에만 집중. 그 첫 작업으로 지난번 보류해뒀던 **구글 드라이브 이미지 저장 기능**을 처음부터 끝까지 구현했다.

### 기능별 구현 기록서 신설
- `PROJECT-RECORD.md` 추가 — `CHANGELOG.md`(날짜순 일지)와 달리 기능/영역별로 "무엇인지 → 왜 이렇게 만들었는지 → 어떻게 구현했는지"를 정리. 폴더를 분리하기 전 전체 맥락을 한 번에 넘겨줄 참고 자료 용도.

### 구글 드라이브 이미지 저장 — 구현
- **왜 드라이브인가**: Supabase 무료 저장 용량(1GB)보다 개인 구글 드라이브(15GB)가 크고 무료. 이미지 실물은 드라이브에, DB엔 링크만 저장 — Supabase 사용량(egress·storage)엔 거의 영향 없음(실측: 파일 스토리지 0GB 유지, egress는 토큰 갱신 요청만큼만 미미하게 증가).
- **계정 연결 방식**: 이메일/비번 계정은 그대로 두고 `supabase.auth.linkIdentity()`로 구글 계정을 "추가 연결"만 함(로그인 수단 교체 아님). `access_type=offline`+`prompt=consent`로 리프레시 토큰까지 확보.
- **`user_drive_tokens` 테이블 신설**: 계정당 리프레시 토큰 하나 저장, RLS로 본인 행만 조회/쓰기/삭제 가능.
- **`refresh-drive-token` Edge Function 신설**: 호출자 JWT 검증 후, 저장된 리프레시 토큰으로 구글 토큰 엔드포인트에서 단기(약 1시간) 액세스 토큰 발급. 클라이언트 시크릿은 Edge Function 환경변수로만 존재.
- **업로드**: Drive의 `multipart` 업로드는 브라우저 기본 `FormData`(=`multipart/form-data`)가 아니라 `multipart/related`를 요구 — 메타데이터(JSON)+파일 바이트를 boundary로 직접 이어붙여 구현. 업로드 후 "링크 있는 사람 보기" 권한 자동 부여, `drive.google.com/thumbnail?id=...` 형태로 `<img>` 삽입.
- **전용 폴더 정리**: 처음엔 "내 드라이브" 최상위에 그냥 쌓였는데, `GRAPHIDEA 이미지` 폴더를 찾거나 없으면 생성해서 그 안에 업로드하도록 수정(폴더 id는 `localStorage`에 캐시).
- **에디터 통합**: 쓰기모드 도구상자에 🖼 버튼(파일 선택) + **클립보드 붙여넣기(Ctrl+V)** 둘 다 지원, 업로드 파이프라인 공통화(`insertUploadedImage`). `renderMarkdown`에 `![alt](url)` 렌더링 추가(기존엔 이미지 문법 자체가 없었음) — 일반 링크 정규식보다 먼저 처리해야 `!`가 링크에 붙어버리는 걸 방지.
- **UX 마감**: 미연결 상태에선 🖼 버튼 자체를 숨김(눌러서 실패 토스트 보는 대신). 설정 → 계정에 연결 상태 표시 + 연결/해제 버튼.

### 구글 드라이브 연동 — 디버깅 과정에서 발견한 실제 버그들
같이 화면 보면서 붙어서 고친 세션이라 시행착오가 그대로 기록에 남는다:
- **"Manual linking is disabled"**: Supabase 프로젝트 설정에서 계정 수동 연결이 기본 꺼져 있어서 발생 — 대시보드에서 켜서 해결(코드 문제 아님).
- **구글 403 "테스터만 액세스 가능"**: OAuth 동의 화면이 테스트 모드라 테스트 사용자로 등록 안 된 계정은 아예 막힘 — 본인 이메일을 테스트 사용자로 추가.
- **localhost로 리다이렉트되며 연결 거부**: `redirectTo`는 배포 URL로 맞춰뒀는데, 로컬(`localhost`) 탭에서 시도해서 발생한 착시였음 — 실제로는 Supabase Site URL/Redirect URLs 설정 자체는 처음부터 정상이었고, 배포된 사이트에서 재시도하니 해결.
- **"구글 드라이브가 연결되어 있지 않습니다" (연결됐다고 나왔는데도)**: `user_drive_tokens` 테이블이 실제로는 비어 있었음(Table Editor로 직접 확인) — 원인은 리프레시 토큰 저장(`saveDriveRefreshTokenFromSession`)이 실패해도 조용히 넘어가던 것. 토스트+콘솔 로그로 실패를 드러내게 고치고 나서 콘솔에서 **"already linked" 에러**를 확인 — 구글 identity는 이미 연결됐는데 토큰 저장만 실패한 "반쯤 연결된" 상태였고, Supabase는 이미 연결된 identity라 재시도해도 동의 화면을 다시 안 띄우고 에러만 냈던 것. 연결 상태 판정을 "토큰 테이블에 행이 있는지"가 아니라 "실제 구글 identity 연결 여부"까지 같이 보도록 고쳐서, 반쯤 연결된 상태면 "연결 해제 후 재시도" 버튼이 뜨게 해 스스로 복구 가능하게 만듦.
- **Edge Function Secrets 이름 실수**: `GOOGLE_CLIENT_ID`를 Name이 아니라 Value 자리에 잘못 넣어서(Name 칸에 클라이언트 ID 값 자체가 들어감) Edge Function이 환경변수를 못 읽던 문제 — 재등록으로 해결.

### 안드로이드 이식
- **구글 OAuth는 앱 내장 웹뷰에서 아예 차단됨**(구글이 보안상 막음) — `@capacitor/browser` 플러그인을 추가해 `skipBrowserRedirect`로 외부 브라우저(크롬)를 띄우고, 커스텀 스킴(`graphidea://drive-callback`)으로 앱에 돌아오면 `appUrlOpen` 이벤트에서 인증 코드를 받아 `exchangeCodeForSession()`으로 세션을 완성하도록 구현. `AndroidManifest.xml`에 해당 스킴 intent-filter 추가.
- **이미지 삽입(갤러리 업로드)은 추가 작업 없이 이미 동작**: 안드로이드 웹뷰가 `<input type=file accept="image/*">`를 만나면 시스템 사진 선택창(갤러리·카메라 포함)을 자동으로 띄워줘서, 웹과 동일한 코드로 실기기에서 확인 완료.
- Supabase Redirect URLs에 `graphidea://drive-callback` 추가는 사용자가 대시보드에서 직접 등록.

### 참고
- **스키마 변경**: `user_drive_tokens` 테이블 신설(리프레시 토큰 저장, select/insert/update/delete 전부 본인 행 한정 RLS). 실 DB에도 반영 완료.
- **새 인프라**: Supabase Edge Function `refresh-drive-token` (대시보드에서 직접 배포), Edge Function Secrets `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`.
- 구글 드라이브 연동은 OAuth 동의 화면이 아직 **테스트 모드**라 등록된 테스트 사용자만 쓸 수 있음(현재 본인 계정만) — 다른 사람 추가하려면 Google Cloud Console에서 테스트 사용자로 등록하면 됨(최대 100명), 심사(verification)는 필요 없음.
- 이미지 화질은 `sz=w2000` 썸네일로 고정 — 모바일 데이터로 사진 많은 노트를 스크롤하면 그만큼 데이터가 나갈 수 있음(노트 단위 지연 로딩 + `loading="lazy"`로 어느 정도 완화는 되어 있음). 필요하면 해상도를 낮추는 것도 가능.

---

## 2026-08-06 작업 요약

캔버스·시트 카드를 중심으로 버그 근본 원인 추적, 시트 카드 기능 고도화, 세 에디터(노트·캔버스·시트)의 상단 레이아웃 통일·모바일 최적화까지 이어진 하루. 웹 기준 전부 반영·푸시, 안드로이드도 매 단계 `cap sync`+재빌드+실기기 확인 후 반영.

### 보안·저장 안정성
- **XSS 속성 탈출 방지**: `escapeHtml`이 `"`/`'`를 이스케이프하지 않아 위키링크 제목·외부 링크 URL로 `data-link`/`href` 속성을 탈출해 삽입할 수 있었음 — 이스케이프 추가로 차단.
- **캔버스/시트 백그라운드 저장 누락 수정**: `visibilitychange`/`pause`/`appStateChange` 핸들러가 일반 노트의 `flushSave()`만 부르고 캔버스(`cevFlushSave`)·시트(`tevFlushSave`)는 안 불러서, 뒤로가기가 아닌 홈 버튼/앱 전환으로 나가면 편집 중이던 캔버스·시트 내용이 유실될 수 있었음 → `flushAllSaves()`로 통합.
- **`schema.sql` 실행 순서 수정**: `deleted_at` 컬럼을 추가하기 전에 그 컬럼을 참조하는 인덱스를 만들고 있어서, 빈 Supabase 프로젝트에 파일을 처음부터 실행하면 에러가 났음 — 컬럼 추가 뒤로 재배치.

### 개인정보처리방침
- **국외이전 조항 신설**: 이전 항목/받는 자(Supabase, Inc.)/국가(싱가포르, AWS `ap-southeast-1`)/시기·방법/목적·보유기간/거부 시 효과 명시.
- **보유기간 항목 보완**: DB에서는 탈퇴 시 즉시 삭제되지만 인프라 제공업체의 백업·로그에는 그쪽 주기에 따라 일정 기간 남을 수 있다는 현실적 단서 추가.

### 캔버스 카드 — 버그 근본 원인 수정
- **원(circle) 크기 조절 불가 버그**: 실제 원인은 리사이즈 계산식이 아니라, 연결용 앵커(`.cev-anchor`)가 상하좌우 리사이즈 핸들과 완전히 같은 좌표에 더 높은 z-index로 얹혀 있었고 `pointer-events`가 안 보일 때도 항상 켜져 있어서 핸들 클릭을 계속 가로채고 있던 것. 원에도 사각형처럼 모서리 포함 8방향 핸들 적용(모서리는 앵커와 안 겹침) + 앵커 `pointer-events`를 보일 때만 켜지도록 수정 + 데스크탑은 도형 선택 시 핸들이 앵커보다 우선하도록 처리(모바일의 "선택 시 앵커로 연결" 동작은 유지).
- **마름모 드래그 생성 시 정사각 미보장**: 리사이즈 핸들 쪽만 정사각을 강제하고 있었고, 드래그로 새로 만들 때는 안 그랬음 — 생성 로직에도 동일한 제약 추가.
- **두 손가락 팬 제스처 고장**: 핀치줌 중 손가락을 같은 방향으로 이동해도 화면이 고정돼 있던 문제 — 제스처 시작 시 손가락 중점 아래 캔버스 좌표를 저장해두고 매 프레임 그 좌표가 현재 중점 아래 유지되도록 팬을 재계산하는 방식으로 수정.
- **캔버스 컨텍스트 툴바 잔류 버그**: 도형 선택 후 다른 카드로 이동해도 그 도형의 툴바가 화면에 남아있던 문제. 최초 수정(`openCanvasEditor`)은 캔버스→캔버스 전환에만 적용됐고, 캔버스→일반노트/시트 전환은 그 함수를 안 타서 안 고쳐졌음 — 모든 카드 전환의 공통 진입점인 `openNote()`로 옮겨 해결.

### 시트 카드 고도화
- **열 타입 변경을 드롭다운으로**: 클릭식 순환 대신 전체 타입을 바로 선택.
- **select 열 옵션 색상**: 태그 색상(`colorForTag`)을 재사용해 옵션·선택된 셀 자체를 채색.
- **모바일 롱프레스 지원**: 행/열 중간 삽입·열 삭제가 우클릭 전용이고 삭제 버튼도 hover 전용이라 모바일에서 안 보이고 안 됐음 — 롱프레스로 컨텍스트 메뉴, 터치에서 삭제 버튼 상시 노출로 수정.
- **필터**: 열 값 기준 행 필터(텍스트 포함/체크박스/연결여부/선택옵션), 필터링된 행 기준으로 푸터 집계도 재계산.
- **'연결' 열 타입 신설**: 셀이 다른 카드를 참조 — 캔버스 카드 피커와 동일한 패턴의 검색형 피커로 연결/변경, 클릭 시 해당 카드로 이동.
- **'계산' 열 타입 신설**: 다른 숫자 열들의 합계/평균/개수를 행 단위로 계산(저장 없이 매번 재계산되는 파생 값).
- CSV 내보내기에서 연결 열은 연결된 카드 제목으로, 계산 열은 계산된 값으로 내보내도록 처리.

### 세 에디터 헤더 구조 통일 + 모바일 최적화
- **캔버스·시트에 노트 카드와 동일한 상단 구조 적용**: 제목(`titlefield`) → 구분선 → 태그 칩 → 도구 상자 순서로 재배치. 캔버스·시트에 **태그(키워드) 편집 UI 신규 추가**(기존 우측 패널의 `rpAddTag`/`rpRemoveTag` 재사용, 자동 동기화).
- **상단바/도구상자 역할 분리**: 노트 카드의 `.toolbar`(고정·삭제·색상 등 액션) 대응 위치에 캔버스는 실행취소/격자스냅/전체보기/확대축소/카드색상/삭제/나가기를, 시트는 가져오기/내보내기/나가기를 배치. 도구상자엔 캔버스는 도형·연결선 타입만, 시트는 필터만 남김.
- **캔버스/시트 상단바-헤더-도구상자 배경 통일**: `--bg-panel`로 세 구역이 하나의 패널처럼 이어지도록.
- **모바일 헤더 축소 (여러 라운드)**:
  - 처음 작성한 모바일 축소 규칙이 파일 앞쪽에 있어 뒤쪽 데스크탑 기본 규칙(같은 우선순위, 나중 규칙이 이김)에 덮여 거의 무효화되고 있던 걸 발견 — 파일 끝으로 옮겨 실제 적용되게 수정.
  - 버튼 줄바꿈이 높이를 잡아먹는 진짜 원인이었음 — 캔버스 도구상자·상단바, 시트 상단바, 노트 카드 `.fmttoolbar`(포맷 버튼 17개, 축소 규칙 자체가 없었음) 전부 한 줄 고정 + 가로 스크롤로 전환.
  - `.mobile-topbar`가 이미 확보한 `safe-area-inset-top`을 `.cev-topbar`/`.tev-topbar`가 모바일에서 또 더하고 있던 이중 계산 버그 발견·수정(캔버스·시트만 상단바가 두 줄 겹치는 유일한 화면이라 발생). 실기기 확인: 헤더 높이가 화면의 약 38% → 18%로 감소.
  - 노트 카드 모바일에서 문서번호·마지막 수정일자 숨김, 글자 수만 표시(읽는 시간도 숨김).
  - 노트 카드 상단 `.toolbar`를 두 줄로 명시적 분리(`.toolbar-row1`/`.toolbar-row2`): 1줄엔 쓰기모드/읽기모드만, 2줄엔 글자수·MD내보내기·복제·카드색상·고정·삭제. 기존엔 폭에 따라 자동 줄바꿈되며 복제 버튼이 화면 밖으로 잘리기도 했음. 데스크탑은 두 그룹 div가 `display:contents`로 사라져 기존과 동일하게 한 줄로 보임.

### 설정 화면
- **화면/편집/성능/데이터/계정** 5개 카테고리로 재구성.
- 신규 항목: 다크모드 토글(사이드바 버튼과 값 공유), 캔버스 격자 스냅 기본값(캔버스를 열 때마다 적용), 그래프 유휴 애니메이션 on/off, 휴지통 자동 비우기(7/30/90일, 휴지통 열 때 기준일 지난 카드 영구 삭제), 로그인 상태에서 바로 쓰는 비밀번호 변경.
- 다크모드에서 네이티브 `<select>` 드롭다운이 흰 배경으로 뜨던 문제 수정(`color-scheme` 지정).

### 참고
- **구글 드라이브 이미지 저장 연동은 논의만 하고 보류** — `IMAGE-STORAGE-PLAN.md`에 나중 착수용으로 정리해둠(액세스 토큰 만료·refresh 필요성, `storage_provider` 컬럼 불필요 판단 등).
- DB 스키마 변경 없음(순서 수정만), 새 인프라 없음. 전부 `www/index.html`·`www/style.css`·`www/lib/constants.js` 수정.
- 문법 검증(`new Function()` 파싱) 매 단계 통과. 실기기(Android) 스크린샷으로 모바일 레이아웃 다수 검증.

---

## 2026-08-05 작업 요약

노트 편집기·모바일·캔버스의 사용성 다듬기 묶음. 웹 기준으로 전부 반영·푸시(안드로이드 앱 반영은 `npx cap sync android` + 재빌드 필요).

### 노트 카드
- **글머리 기호·번호 매기기 툴바 버튼 제거** (`fmtBulletList`/`fmtOrderedList` 버튼·핸들러 삭제; `toggleBulletList`/`toggleOrderedList` 함수는 미사용으로 남겨둠).
- **콜아웃을 툴박스 선택 방식으로**: 💡 버튼을 누르면 4종(📝노트·💡팁·⚠️주의·🚫위험) 팝오버가 뜨고 하나를 골라 삽입. 캔버스 컨텍스트 툴바와 유사한 형태(`openCalloutPicker`, `CALLOUT_TYPES`, `.callout-picker` CSS). 기존 `insertCallout()`은 타입 인자를 받도록 변경.
- **쓰기|읽기 분할 구분선을 터치로도 조절**: `#splitDivider` 드래그에 touch 이벤트 추가(태블릿/펜), `touch-action:none` + coarse 포인터에서 손잡이 폭 22px.
- **다크모드 텍스트 드래그(선택) 하이라이트를 흰색으로**: `[data-theme="dark"] ::selection{ background:#fff; color:#1F1E1B }` (기존 accent-soft가 어두운 배경에서 거의 안 보였음).
- **읽기모드 2카드 비교 (신규)**: 읽기모드 툴바에 `⧉ 비교` 버튼. 누르면 읽기 영역이 좌(현재 노트)|우(피커로 고른 노트)로 갈라짐. 가운데 드래그 구분선(비율 `localStorage: stk-compare-ratio`), 각 창 독립 스크롤, ✕로 닫기, 오른쪽 창의 위키링크는 같은 창에 로드. 노트 타입 카드만 피커에 노출. **데스크탑/태블릿 전용**(≤640px에선 버튼 숨김·한 장 유지). 관련: `compareOpen`/`compareId` 상태, `renderComparePane`/`openComparePicker`, `.editor-split.compare` CSS, `#comparePane`/`#previewBox2`/`#compareDivider` DOM.

### 모바일 / 기타
- **사이드바 오버레이 열린 상태에서 콘텐츠 영역 탭 → 패널 닫힘**: 캡처 단계 클릭 핸들러가 그 탭을 소비해 밑의 카드가 실행되지 않도록 처리(≤640px 한정).
- **Android 뒤로가기 지원 (갤럭시 등)**: Capacitor `App` `backButton` 리스너 + `handleBackNav()` — 팝오버→모달(가이드/설정)→모바일 오버레이(오른쪽 패널·사이드바)→열린 뷰(편집분 flush 후 홈+목록) 순으로 한 단계씩 해제, 더 해제할 게 없으면 `minimizeApp()`(앱 종료가 아니라 백그라운드로).
- **로그인/재설정 입력창 Enter로 제출**: 이메일·비번·새비번 필드에서 Enter → 해당 버튼 클릭(모바일 키보드의 "이동/Go" 키 포함).

### 캔버스 카드
- **마름모 리사이즈 시 항상 정사각(네 변 길이 동일)**: diamond는 원처럼 폭==높이로 균일 확대되도록 리사이즈 분기 추가(드래그한 핸들의 반대편을 고정 앵커로). 이전엔 w≠h면 회전 사각형이 되어 네 변이 두 쌍으로 갈렸음.
- **도형 선택 후 Enter로 바로 글씨 입력**: 캔버스 keydown에 Enter 처리 추가(`cevBeginTextEdit` — 더블클릭과 동일하게 텍스트/라벨 편집 진입). 단일 선택일 때만.
- **모바일 선연결 개선**: 근본 원인 2건 수정 — ① 앵커 점에 `touchstart`가 아예 없어서 터치로는 연결 시작 자체가 불가했음(추가; document touchmove/touchend는 이미 `cevConnDrawing` 처리 중이라 나머지는 그대로 동작). ② 터치엔 `:hover`가 없어 `.cev-el:hover > .cev-anchor`가 영원히 안 떴음 → **선택된 도형에 앵커 표시**(`.cev-el.selected > .cev-anchor`)로 바꾸고 coarse 포인터에서 22px로 확대. (요청대로 탭-탭 연결모드 등 추가 단계 없이 Stage 1에서 종료.)

### 참고
- 이번엔 **DB 스키마 변경 없음**, 새 인프라 없음. 전부 `www/index.html`·`www/style.css` 두 파일 수정. 저장 키 추가: `stk-compare-ratio`.
- 문법 검증(인라인 스크립트 `new Function()` 파싱) 통과. 브라우저 확장 미연결·자격증명 부재로 실사용 시각 검증은 배포 후 로그인해 확인 필요.

---

## 2026-08-04 작업 요약

개인용 노트 앱에서 **유료·다중 사용자 정식 출시**로 방향을 굳힌 날. 웹 기능을 먼저 다 정리하고, Play 스토어 출시 전 체크리스트를 전부 처리하고, 실제 계정 2개로 RLS를 검증하고, 마지막에 안드로이드 앱까지 오늘 변경분을 전부 반영했다.

### 그래프 기반 롤업 프로퍼티
- 노션의 relation+rollup을 별도 relation 필드 없이 구현 — 이미 있는 위키링크(`[[ ]]`) 그래프를 그대로 relation처럼 사용(`connectedNotesOf`: 나가는 링크+들어오는 백링크 전부). 프로퍼티 타입에 "Σ 롤업" 추가, 연결된 카드 중 지정한 이름의 숫자 프로퍼티를 합계/평균/개수로 집계(`computeRollup`).
- 롤업은 다른 롤업 프로퍼티를 읽지 않도록 설계해서 순환 참조가 애초에 불가능(A→B→A 롤업 루프 걱정 없음).

### 버그 수정
- **마크다운(.md) 불러오기가 조용히 아무 일도 안 하던 문제**: `input.files`가 라이브 참조 객체라, 캡처해둔 파일 목록을 `e.target.value=''`로 초기화하면 캡처본까지 같이 비워지던 버그. `Array.from()`으로 스냅샷을 떠서 해결.
- **카드 진입 시 항상 맨 위에서 시작하던 문제**: 노트별 커서 위치·스크롤 위치를 `localStorage`에 기억해뒀다가 재진입 시 복원.
- **캔버스 카드에 연결할 노트를 고르는 드롭다운이 겹쳐 보이던 문제**: `.cev-card-picker-item`이 `overflow:hidden`을 쓰면 flex 자식의 최소 높이가 0으로 계산되는 CSS 특성 때문에, 후보가 많아지면 flexbox가 각 행을 강제로 짓눌러 14px까지 찌그러뜨리고 있었음. `flex-shrink:0` 추가로 해결, 최대 7개 정도 보이고 나머지는 스크롤.

### 그래프 아이들 플로팅
- 대시보드 히어로 그래프·전체 그래프·우측 패널 미니 그래프(연결 지도) 세 곳 모두, 힘-시뮬레이션이 멈춘 뒤에도 완전히 정지하지 않고 노드당 2~4px 반경·7~12초 주기의 느린 사인파로 계속 표류하도록 추가(`startIdleFloat`). 드래그하면 즉시 실제 물리 시뮬레이션으로 복귀.

### Play 스토어 출시 전 체크리스트 — 전부 완료
- **Supabase SDK 번들 내장**: jsdelivr CDN 미고정(`@2`) 버전 대신 `npm install @supabase/supabase-js`로 정확한 버전을 고정해 `www/vendor/supabase.js`로 번들. 버전 올릴 땐 `npm run vendor:supabase`.
- **페이지네이션 → 본문 지연 로딩으로 방향 전환**: 행 단위 페이지네이션 대신, `notes` 테이블에 `type`(note/canvas/table) 컬럼과 `content_len`(generated) 컬럼을 추가해 목록·그래프·검색·위키링크 자동완성은 가벼운 필드만 불러오고, 본문(content)은 카드를 열 때만 개별 요청(`ensureContentLoaded`). 리네임 시 다른 카드의 `[[링크]]` 갱신, 캔버스 백링크 탐색처럼 원래 전체 노트를 스캔하던 기능은 서버 쪽 `ilike` 부분일치 쿼리로 후보만 좁혀서 조회하도록 재작성. 알려진 트레이드오프: 이번 세션에서 아직 한 번도 안 연 노트의 본문은 검색에 안 걸림(제목·태그 검색은 영향 없음).
- **개인정보처리방침·이용약관**: `www/privacy.html` 신설, 로그인 화면과 설정 모달에서 링크.
- **비밀번호 재설정 딥링크**: `redirectTo: window.location.href`가 앱 안에서는 `file://`/`capacitor://` 주소라 재설정 이메일의 브라우저가 못 열던 문제 — 고정된 배포 URL(`SITE_URL`)로 교체해 최소한 브라우저에서는 항상 완료되도록 수정.
- **회원 탈퇴**: 설정 모달에 버튼 추가. anon key로는 auth 계정을 못 지우므로(관리자 권한 필요) Supabase Edge Function(`supabase/functions/delete-account`)을 신설 — 호출자 JWT를 검증한 뒤 service-role로 본인 계정만 삭제, 노트·체크리스트는 `auth.users` FK의 `on delete cascade`로 자동 정리. 배포 직후 curl로는 되는데 브라우저에서 "Failed to fetch"가 나서 확인해보니 CORS 헤더 누락이었음 — 프리플라이트 처리 및 헤더 추가로 해결.

### 유료·다중 사용자 대비
- **RLS 실제 계정 2개로 교차 검증**: 테스트 계정을 새로 만들어 기존 30여 개 노트가 전혀 안 보이는 것, 새로 만든 노트가 그 계정에만 귀속되는 것, 회원 탈퇴 후 재로그인이 실패하는 것까지 실측 확인. (검증 도중 같은 브라우저 탭이 로그인 세션을 공유한다는 걸 몰라서 실제 계정 세션이 테스트 계정으로 잠깐 덮어써짐 — 데이터 손실은 없었고 재로그인으로 복구.)
- **`user_id` 인덱스 추가**: RLS가 모든 쿼리를 `user_id = auth.uid()`로 암묵적으로 필터링하는데, Postgres는 FK 컬럼을 자동 인덱싱하지 않아 사용자가 늘면 매번 풀스캔이었음. `notes(user_id, deleted_at, updated_at desc)`, `checklist_items(user_id, created_at desc)` 복합 인덱스로 해결.
- **전체 백업 다운로드**: 노트 1개씩 md 내보내기만 있던 걸, 설정 → 데이터에서 모든 카드+할 일을 JSON 파일 하나로 한 번에 내려받도록 추가.

### 유지보수 — 파일 구조 분리 (1단계)
- `index.html` 6944줄 → 6070줄. `<style>` 블록을 `www/style.css`로, 공유 상태(`index`, `currentId`, `sb` 등)를 전혀 참조하지 않는 순수 함수·상수(색상 팔레트, 마크다운 이스케이프, CSV/캔버스/시트 콘텐츠 파서, 노트 타입 판별 등)를 `www/lib/constants.js`·`www/lib/utils.js`로 분리. 전부 일반 `<script>` 태그라 기존 호출부는 한 곳도 안 건드림(전역 함수 선언이 같은 IIFE 안에서 밖으로 옮겨진 것뿐). 캔버스 에디터·그래프·대시보드·실시간 동기화처럼 상태와 강하게 얽힌 로직은 이번엔 그대로 뒀음 — 억지로 쪼개면 회귀 위험이 커서, 나중에 기능 단위로 점진적으로.

### 안드로이드 이식
- 오늘 웹에서 만든 변경분 전부를 `npx cap sync android` → `gradlew assembleDebug` → 실기기(갤럭시 S25) 설치로 반영. 로그인 화면(개인정보처리방침 링크), 노트 목록(지연 로딩·타입별 아이콘), 에디터, 대시보드(그래프·글자수 통계), 설정 모달(백업·회원탈퇴), 전체 백업 다운로드까지 실기기에서 전부 정상 동작 확인.

### 참고
- `schema.sql` 변경 있음(`type`/`content_len` 컬럼, `user_id` 복합 인덱스 2개) — 실 DB에도 반영 완료.
- 새 인프라: Supabase Edge Function(`supabase/functions/delete-account`) — Supabase 대시보드 Edge Functions에서 직접 배포(코드는 CLI 없이 대시보드 에디터에 붙여넣는 방식으로 관리).
- 오늘부로 Play 스토어 정식 출시 전 필수 항목은 전부 처리됨. 남은 건 `appId` 확정, 릴리스 키스토어, 스토어 등록정보(스크린샷/설명), Play Console 개발자 계정 — 실제 제출 시점에 처리.

---

## 2026-07-30 작업 요약

**안드로이드 앱 빌드 시작** — 개인적으로 계속 써보다가 추후 Play 스토어 정식 출시를 목표로, Capacitor로 이 웹앱을 감싸는 작업을 시작. 자세한 절차·환경 세팅·다음에 이어할 것은 [`CONTINUE-ON-ANOTHER-COMPUTER.md`](CONTINUE-ON-ANOTHER-COMPUTER.md)의 "안드로이드 빌드 (Capacitor)" 절 참고.

### 저장소 재구조화 (Capacitor 도입)
- `index.html`/`icon.png`를 저장소 루트 → `www/`로 이동(히스토리 보존). `www/index.html`이 웹·앱 공용 소스 — 앱 따로 웹 따로가 아니라 동일 코드베이스를 두 경로로 배포.
- `package.json`, `capacitor.config.json`(appId `com.specialbee.graphidea`, 임시값), `vercel.json`(`outputDirectory: "www"`) 추가.
- `npx cap add android`로 `android/` 네이티브 프로젝트 생성, 저장소에 커밋(Capacitor 표준 관행).
- `@capacitor/assets`로 `icon.png` 기반 안드로이드 아이콘·스플래시(라이트/다크) 일괄 생성.

### 보안 · 안정성
- **저장형 XSS 수정**: 대시보드 "오늘의 한 장" 미리보기가 노트 본문을 `escapeHtml` 없이 `innerHTML`에 꽂던 버그. 전체 파일 재점검해서 동일 유형의 다른 취약점 없음을 확인.
- 앱이 백그라운드로 전환될 때(`visibilitychange`, Capacitor `App` pause/appStateChange) 대기 중이던 디바운스 저장을 강제로 flush — 모바일에서 앱 전환/OS 강제종료로 방금 쓴 내용이 날아가는 걸 방지.

### Android 15 edge-to-edge 버그 수정
- 실기기/에뮬레이터에서 **상단 햄버거 메뉴 등 최상단 버튼이 터치가 안 먹는 버그** 발견 — Android 15(API 35+) 기본 edge-to-edge 렌더링으로 앱 콘텐츠가 상태표시줄 영역까지 그려지는데, 그 영역 터치는 시스템이 가로채서 생긴 문제.
- `viewport-fit=cover` + 상단에 고정되는 모든 바(`.mobile-topbar`, `.toolbar`, `.brand`, `.tev-toolbar`, `.cev-toolbar`, `.graph-header`, 모바일 오른쪽 패널 오버레이, `#dashboardView`)에 `env(safe-area-inset-top)` 여백 추가로 해결.
- 에뮬레이터 빌드에 `adb shell input tap`으로 직접 탭까지 시켜서 화면별로 실측 검증(홈/대시보드/데이터베이스/설정/노트 편집기/오른쪽 패널/전역 그래프).

### 대시보드 히어로 그래프 레이아웃
- 좌측 그래프·우측 통계·범례 배치를 **상하 배치**(그래프 위, 통계·범례·버튼 한 줄이 아래)로 변경. 데스크톱·모바일 동일 규칙이라 화면 크기 구분 없이 일괄 적용.

### 에디터 부가 기능
- 사이드바 태그 목록 **10개 제한 + "…" 팝업**(초과 태그 전체 보기).
- 쓰기모드 미리보기 **더블클릭 → 편집기 커서 이동**(비례 위치 근사 + 단어 경계 스냅).
- 설정에 **카드 글자 크기**(축소/보통/확대), **쓰기모드 레이아웃**(쓰기모드만 / 쓰기모드+미리보기) 추가.
- 마크다운 표 **정렬**(헤더 클릭으로 좌/가운데/우 순환, 표준 `:---`/`:---:`/`---:` 문법) · **칸 배경색**(비표준 `{{bg:#hex}}` 마커, 미리보기에서 색칠 버튼으로 편집) 지원.
- 오른쪽 패널 미니 연결 지도가 전역 그래프처럼 **라이브**해짐 — 현재 카드 노드도 드래그 가능, 이웃 노드 클릭 시 이동.

### 현재 진행 상황 (미완료)
- 에뮬레이터(`graphidea_test`, API 35)로 첫 빌드·설치·실행 성공, 실사용 테스트 통과.
- 실제 기기(갤럭시 S25) 연결 진행 중 — 삼성 USB 드라이버까지 설치해 Windows는 기기를 인식하나, 개발자 옵션의 "USB 디버깅" 스위치가 회색 비활성화 상태로 막혀 있음. 다음 세션에서 이어서 진행.

### 참고
- `schema.sql` 변경 없음 — DB 스키마는 그대로, 이번 작업은 전부 배포 구조·프론트엔드.
- Play 스토어 정식 출시 전 남은 것: 없음 (2026-08-04부로 전부 완료 — 아래 항목들 참고).
  - ~~Supabase SDK 번들 내장~~ 2026-08-04 완료 — jsdelivr CDN 미고정 버전 대신 `www/vendor/supabase.js`에 정확한 버전 고정해 번들.
  - ~~노트 목록 페이지네이션~~ 2026-08-04 완료(방향 전환) — 행 단위 페이지네이션 대신 본문(content)만 지연 로딩으로 전환. `notes.type`/`content_len` 컬럼 추가, 목록·그래프·검색·위키링크는 가벼운 필드만 로드하고 본문은 카드를 열 때 개별 요청. 알려진 트레이드오프: 아직 한 번도 안 연 노트의 본문은 이번 세션에서 검색에 안 걸림(제목·태그 검색은 그대로 동작).
  - ~~개인정보처리방침/이용약관~~ 2026-08-04 완료 — `www/privacy.html` 신설, 로그인 화면·설정 모달에서 링크.
  - ~~비밀번호 재설정 딥링크~~ 2026-08-04 완료 — `redirectTo`를 `window.location.href`(앱 안에선 file://·capacitor:// 라 깨짐) 대신 고정된 배포 URL로 변경.
  - ~~회원 탈퇴~~ 2026-08-04 완료 — 설정 모달에 버튼 추가, Supabase Edge Function(`supabase/functions/delete-account`)이 본인 인증 후 계정을 삭제(노트/체크리스트는 FK cascade로 자동 삭제). 대시보드에서 배포 완료, CORS 헤더 누락 문제 한 번 발견·수정 후 브라우저에서 정상 동작 확인.

---

## 2026-07-29 작업 요약

### 쓰기 모드 — 미리보기 자동 스크롤
- 분할(split) 모드에서 왼쪽 에디터에 입력할 때마다 오른쪽 미리보기가 **최하단으로 자동 스크롤**되도록 수정. 이전엔 미리보기가 스크롤 위치를 그대로 유지해, 긴 문서 끝부분을 작성할 때 결과를 보려면 매번 수동으로 내려야 했음.
- 모바일에선 애초에 분할 모드에서 미리보기 패널 자체를 숨기고 에디터만 전체폭으로 보여주는 구조(2026-07-28 모바일 레이아웃 수정 참고)라, 이번 변경은 데스크톱 분할 화면에만 적용되고 모바일엔 영향 없음.
- **후속 버그 발견 및 수정**: 위 자동 스크롤이 서식 툴바 버튼 클릭에도 반응해, 문서 중간을 굵게/하이라이트 처리할 때마다 미리보기가 편집 중이던 위치를 벗어나 맨 밑으로 튕겨나가던 문제 발생(피드백 스크린샷으로 확인). `input` 이벤트가 실제 키 입력(`isTrusted:true`)인지 툴바가 보낸 합성 이벤트(`isTrusted:false`)인지로 구분해, 최하단 추적은 실제 타이핑에서만 동작하도록 1차 수정.
- **2차 수정(방식 전면 교체)**: isTrusted 구분만으로는 부족했음 — 이미 맨 밑 근처에서 작업 중이면 툴바 클릭이어도 여전히 맨 밑으로 붙는 경우가 남아있었음. "최하단 추적" 자체를 버리고 **커서가 문서 전체에서 차지하는 위치 비율만큼 미리보기도 그 비율 지점으로 스크롤**하는 방식으로 교체(편집 이벤트마다 재계산). 문서 중간을 편집하면 미리보기도 중간쯤을 보여주고, 끝을 편집하면 끝을 보여줌. **설정 → 쓰기 모드**에 켜고 끌 수 있는 토글 추가(기본 켜짐).

### 서식 툴바 — 편집기(왼쪽) 화면도 같이 튀던 문제 수정
- 미리보기(오른쪽)만 고친 뒤에도 편집기(왼쪽) 자체가 여전히 서식 적용 시 스크롤이 튀는 문제가 남아있었음(피드백으로 확인). 원인은 `textarea.value`를 재할당하면 스크롤이 초기화되고, 이어지는 `focus()` + 커서 위치 재설정 과정에서 브라우저가 자체적으로 스크롤을 이동시키는 것.
- 굵게/리스트/들여쓰기/링크/코드/표/구분선 등 서식 기능 전부가 거치는 값 변경 지점을 `setEditorValue()` 공통 함수로 묶어, 값을 바꾸기 직전 스크롤 위치를 저장했다가 변경 후 그대로 복원하도록 수정.

### 다크 모드 — 하이라이트 글자색 대비 수정
- 마크다운 하이라이트(`==텍스트==`) 배경은 밝은 프리셋 색상인데 글자색은 테마의 `--ink`(다크모드에선 밝은 크림색)를 그대로 써서, 다크 모드에서 밝은 배경 위에 밝은 글자가 겹쳐 안 보이던 문제 발견.
- 하이라이트는 실제 형광펜처럼 **테마와 무관하게 항상 밝은 배경 + 어두운 고정 글자색**(`--mark-ink`)을 쓰도록 변경. 커스텀/프리셋 색상 어떤 걸 골라도 다크 모드에서 항상 읽힘.

### 설정 기능 신설
- 사이드바 상단에 ⚙️ 버튼 → 설정 모달 추가(기존 사용법 모달과 동일한 레이아웃 재사용).
- **테마 색상**: 프리셋 8개 + 커스텀 컬러피커. `--accent`를 바꾸면 `--accent-hover`/`--accent-soft`를 라이트/다크 테마별로 다르게 자동 혼합(mix) — 다크 모드 전환 시에도 재계산됨.
- **글꼴**: 시스템 폰트 5종(기본 Inter · 시스템 UI · 맑은 고딕 · 바탕(명조) · 고정폭) 드롭다운. 별도 네트워크 로딩 없이 OS 기본 폰트만 사용.
- **마크다운 하이라이트(`==텍스트==`) 색상**: 테마 색상과 별개로 프리셋 6개 + 커스텀 지정 가능(`--mark-bg` 변수 신설, 기존엔 `--accent-soft`에 종속).
- **쓰기 모드 — 미리보기 동기화 토글**: 위 커서 위치 동기화 기능을 켜고 끌 수 있는 체크박스.
- 전부 `localStorage`에 저장되어 새로고침 후에도 유지, 모바일 좁은 화면에서도 모달 정상 표시 확인.

### 서식 툴바 — 토글 버그 수정
- 굵게/기울임/취소선/하이라이트 버튼을 같은 부분에 두 번 누르면 `**`/`==` 등이 계속 겹쳐 늘어나던 문제 수정. 이제 이미 서식이 적용된 선택 영역에 다시 누르면 서식이 해제(toggle off)됨.

### 카드 목록 — 컴팩트 모드 시간 숨김
- 축소(컴팩트) 밀도로 볼 때는 카드에 표시되던 수정 시각을 숨겨 한 줄에 더 집중되게 함.

### 노트 편집 UX 점검 — 실행취소 / Tab 들여쓰기 / 위키링크 자동완성 / 제목 Enter
- 코드 리뷰로 노트 편집에서 불편할 만한 지점을 미리 찾아 4가지 처리(제보된 버그가 아니라 선제적 점검).
- **서식 버튼이 실행취소(Ctrl+Z) 기록을 깨뜨리던 문제**: 굵게/리스트/들여쓰기 등 모든 서식 기능이 `textarea.value`를 스크립트로 직접 덮어쓰는 방식이라 브라우저 네이티브 undo 스택이 그 지점에서 끊겼음(캔버스 에디터엔 자체 Undo/Redo가 있지만 일반 마크다운 카드엔 없었음). `document.execCommand('insertText', …)`로 실제 편집 명령을 통해 값을 바꾸도록 전면 교체해 서식 적용 후에도 Ctrl+Z가 정상 동작하도록 수정(미지원 브라우저 대비 값 직접 교체 폴백 유지).
- **Tab 키로 들여쓰기**: 리스트 쓰다가 Tab을 누르면 (브라우저 기본 동작으로) 포커스가 에디터 밖으로 빠져나가던 문제. 이제 Tab/Shift+Tab이 현재 줄(들)을 들여쓰기/내어쓰기.
- **위키링크(`[[제목]]`) 자동완성 신설**: `[[`를 입력하면 커서 근처에 기존 카드 제목 드롭다운이 뜸(캐럿의 화면 좌표를 구할 네이티브 API가 없어 미러 `<div>` 기법으로 계산). 입력한 글자로 실시간 필터링, ↑/↓ 탐색, Enter/Tab/클릭으로 선택 시 `제목]]` 자동 완성. 일치하는 카드 없으면 "새 카드로 연결됨" 안내. 모바일 좁은 화면에서 팝업이 화면 밖으로 안 나가도록 위치 클램핑 처리.
- **제목 입력창 Enter → 본문으로 이동**: 그동안 제목에서 Enter를 눌러도 아무 일도 없었음. 이제 본문 맨 앞으로 커서 이동.

### 참고
- 미리보기 자동 스크롤은 한 날 동안 세 번(최하단 고정 → isTrusted 구분 → 커서 비율 동기화) 방식을 바꿨음. 매번 실사용 피드백으로 이전 수정의 한계가 드러난 케이스 — 앞으로 스크롤/포커스 관련 기능은 "일단 되는 것 같다"보다 여러 편집 위치(맨 위/중간/맨 밑)에서 직접 확인하고 넘기기로 함.
- 노트 편집 UX 점검 후 남겨둔 항목(다음 후보): `[[ ]]` 자동완성과 짝을 이루는 이미지 붙여넣기/드래그드롭 지원(Supabase storage 버킷 설정 필요), 긴 문서에서 키 입력마다 미리보기 전체 재렌더링하는 부분의 디바운스 여부.

---

## 2026-07-28 작업 요약

피드백 기반 기능 추가 이후, 모바일에서 아예 못 쓰던 걸 근본 원인부터 고쳐서 실사용 가능한 수준으로 만든 날.

### 전역 그래프도 라이브 시뮬레이션으로
- 한 번 계산하고 멈추는 정적 레이아웃 → 히어로 그래프와 같은 **alpha-냉각 힘-시뮬레이션**으로 전환. 열 때 저장된 위치에서 시작해 살짝 정리되며 정착(재배치 아님). 드래그하면 나머지 노드가 물리적으로 반응, 정착/드래그 위치는 그대로 `localStorage`에 저장.

### 기능 추가 모음
- **마크다운(.md) 불러오기** — "+ 새 카드 만들기" 메뉴에서 여러 파일 한 번에 임포트(파일명=제목, 본문의 태그/링크 자동 인식).
- **캔버스/시트 카드 태그 편집** — 우측 패널 태그 섹션에 추가/삭제 UI 신설(이전엔 두 카드 종류엔 태그 편집 수단 자체가 없었음).
- **시트(테이블) 파괴적 작업에 확인창** — 열 타입 변경(값 있을 때만), 열 삭제, 행 삭제(앞 칸 버튼 + 우클릭 메뉴 전부).
- **캔버스 도형 드래그 생성** — 도형 툴로 드래그하면 드래그한 크기로 즉시 생성(클릭만 하면 기존처럼 기본 크기).
- **다크모드 캔버스 도형 글씨** — 배경은 테마 무관 고정 파스텔인데 글씨만 테마색을 써서 다크모드에서 밝은 글씨+밝은 배경으로 안 보이던 문제 수정.

### 카드 목록 UX
- **밀도 토글** — 정렬 버튼 옆 "☰"로 기본/컴팩트 전환(컴팩트는 카드당 높이 절반, 화면에 더 많이 보임). `localStorage`에 기억.
- **자동저장 시 목록 전체가 깜빡이던 문제 수정** — 카드 순서/구성이 실제로 안 바뀌면 페이드인 애니메이션을 스킵(내용만 갱신되는 저장·태그변경·체크 토글 등에서 리스트가 조용해짐).

### 대시보드 체크리스트 클라우드 동기화
- 유일하게 `localStorage`에만 있던 사용자 콘텐츠였던 "할 일" 위젯을 `checklist_items` 테이블(노트와 동일한 사용자별 RLS 패턴)로 이전. 기존 로컬 항목은 첫 동기화 때 자동으로 클라우드에 마이그레이션.

### 모바일 레이아웃 — 근본 원인 수정
- **원인**: `applyPanelState()`가 화면 전환마다 `appEl.style.gridTemplateColumns`를 인라인으로 강제 설정 → 인라인 스타일이 항상 CSS/미디어쿼리보다 우선이라, 이미 작성돼 있던 640px/900px 반응형 규칙이 **한 번도 실제로 적용된 적이 없었음**. `.sidebar`의 모바일 `display:none`도 같은 이유(동일 우선순위 동점 시 나중에 나오는 무조건 규칙이 이김)로 무력화돼 있었음.
- 좁은 화면에서도 사이드바가 항상 280px를 차지해 나머지 콘텐츠가 글자 한 자씩 줄바꿈될 정도로 쥐어짜이던 상태 → `applyPanelState()`가 `window.innerWidth`를 직접 읽어 모바일/태블릿/데스크톱 그리드를 분기하도록 수정, 리사이즈·회전에도 반응. CSS 특이도도 상향해 미디어쿼리가 실제로 이기게 함.
- 캔버스/테이블/전역그래프/대시보드/데이터베이스 화면엔 사이드바로 돌아갈 방법(햄버거 버튼)이 아예 없었음(노트 편집 화면에만 있었음) → 공용 모바일 상단바 신설, 카드 선택 시 자동 닫힘.
- 화면별 점검에서 두 개 더 발견: 노트 편집 **분할 모드**(쓰기모드)가 375px에서도 반반으로 쪼개져 읽기 불가 → 모바일에선 에디터만 전체폭으로; **캔버스 액션 버튼**(실행취소·확대축소·나가기 등)이 화면 밖으로 넘쳐 손도 못 대던 문제 → 줄바꿈되도록 수정.

### 터치 입력 지원
- 마우스 전용이던 **전역 그래프**(팬/노드드래그/휠줌), **대시보드 히어로 그래프**(노드드래그), **캔버스 에디터**(도형 이동·리사이즈·마퀴선택·드래그생성·팬·줌)에 터치 대응 추가. 한 손가락 = 마우스 좌클릭 동작 재사용(핸들러를 이름 있는 함수로 리팩터해 로직 중복 없이 터치 이벤트가 동일 코드 경로를 타도록 함), 두 손가락 = 핀치줌+팬(마우스엔 없던 제스처라 신규 작성).
- **의도적으로 제외**: 캔버스 커넥터(화살표) 그리기는 호버로만 나타나는 UI라 터치에 그대로 옮길 수 없어 별도 UX 설계가 필요 — 이번엔 보류.

### 마크다운 편집 기능 확장
- 서식 툴바에 **글머리 기호/번호 매기기 리스트, 들여쓰기/내어쓰기, 하이라이트(`==텍스트==`)** 추가. 미리보기 렌더러도 들여쓰기 기반으로 실제 중첩 리스트(`<ul>`/`<ol>` 트리)를 그리도록 새로 작성.
- 버그 2건 수정: 들여쓰기가 리스트가 아닌 일반 문단엔 시각 효과가 전혀 없던 문제(렌더러가 줄 앞뒤 공백을 무조건 `trim()`), 들여쓴 줄 전체를 굵게/하이라이트 등으로 감싸면 들여쓰기가 사라지던 문제(`wrapSelection`이 선행 공백까지 태그 안에 넣어버림 → 들여쓰기 판정이 안 됨).

### 원칙 확립 — 모바일 우선
- "앞으로 모든 작업은 모바일 버전도 함께 확인" 을 1원칙으로 메모리에 저장. 이후 기능마다 모바일 뷰포트(390px)+터치로 별도 검증하는 걸 기본 절차로 편입.

### 우측 패널 "연결 지도" — 라이브 시뮬레이션
- 정적 원형 배치 → 전역/대시보드 그래프와 같은 alpha-냉각 힘-시뮬레이션으로 전환. 현재 카드는 중앙 고정, 이웃은 반발+스프링으로 정착, 마우스/터치 드래그 가능.
- 배포 직후 "안 움직이는 것 같다" 피드백 → 이웃 노드를 처음부터 최종 위치(반지름 55, 스프링 이상 길이와 거의 동일)에 배치해서 물리적으로 움직일 이유가 거의 없었던 게 원인. 중심 근처에서 무작위로 시작해 퍼져나가도록 수정해 체감 가능한 움직임 확보.

### 전역 그래프 — 유사-3D(fake 3D) 깊이 효과 프로토타입
- 새 라이브러리 없이 기존 2D 힘-시뮬레이션 위에, 노트 id 기반 고정 깊이값을 부여해 크기·투명도·블러·그리기 순서로 원근감 연출. 그래프 헤더에 "입체 효과" 토글(기본 ON, 꺼도 이전 렌더링과 완전 동일해 A/B 비교 가능).
- **배포 직후 심각한 회귀 발생 및 긴급 수정**: 깊이 해시 함수의 XOR 결과가 부호 있는 정수라 종종 음수가 나왔고, 그러면 깊이 버킷을 못 찾아 `undefined.scale`에서 렌더링 함수 전체가 SVG를 그리기도 전에 죽어 그래프가 통째로 사라짐. 노트 수가 적은 테스트에선 우연히 안 걸렸다가 실사용 규모에서 재현됨 — 해시를 부호 없는 정수로 강제하고 버킷 탐색에 폴백 추가, 80개 랜덤 노트 5회 반복 스트레스 테스트로 재발 없음 확인.

### 참고
- `schema.sql` 변경 있음(`checklist_items` 테이블 신규) — 실 DB에도 반영 완료.
- 검증: Playwright로 모바일/태블릿/데스크톱 3개 폭 + 회전 시뮬레이션, `Touch`/`TouchEvent` 합성 디스패치로 팬·노드드래그·핀치줌·탭·도형이동·리사이즈·드래그생성 전부 실제 상태 변화 확인, 데스크톱 마우스 경로 회귀 없음 재확인.
- 오늘 배포한 기능 중 2건(연결 지도 애니메이션, 그래프 3D 깊이 효과)이 작은 테스트셋 검증만으로는 못 잡는 문제로 배포 후 발견·핫픽스됨 — 그래프/리스트류처럼 데이터 규모에 민감한 기능은 이후 수십~수백 개 랜덤 데이터로 스트레스 테스트를 병행하기로 함.

---

## 2026-07-27 작업 요약

QA·피드백 단계. 죽은 코드/이름 충돌을 정리하고, 대시보드를 "카드를 얼마나 쌓았나"가 아니라 **"생각을 얼마나 연결했나"** 중심으로 재설계했으며, 실시간 동기화 버그와 그래프 상호작용을 손봤다.

### 정리 · 죽은 코드 제거
- 어디서도 호출되지 않던 **옛 "보드" 캔버스 뷰 완전 삭제**(HTML/CSS/JS ~450줄). 현행 캔버스 에디터(`cev`)로 이미 대체된 잔재. 미사용 `canvas_sections` 테이블 쿼리 + `canvas_x/y/color` 노트 필드까지 제거.
- 레거시 ` ```collection ` 노트 감지/리다이렉트 코드 제거(해당 카드 없음 확인).
- **"데이터 카드" → "시트 카드"** 개명 — 전역 뷰 "🗄 데이터베이스"와의 이름 충돌 해소(메뉴·에디터·배지·가이드 전반).
- 캔버스/시트/데이터베이스 뷰 나가기 버튼 문구를 **"← GRAPHIDEA로 돌아가기"** 로 통일.
- 내부 식별자 `collection`/`cov` → `database`/`dbView` 리네이밍(기능 동일, 유지보수성).
- `schema.sql`에서 캔버스 컬럼·테이블 제거(주석 `Stacker`→`GRAPHIDEA`), **실 DB에도 DROP 실행**.

### 자동저장 데이터 유실 수정
- 캔버스 편집 중 저장 debounce(600ms) 전에 다른 카드로 이동하면 마지막 편집이 유실되던 문제. `openNote()`·`exitCanvasEditor()`에 `cevFlushSave()` 추가(시트 에디터엔 이미 있던 패턴).

### 대시보드 재설계 — 그래프 중심 (3단계)
- **문제의식**: 레벨/XP가 사실상 "삭제 안 된 카드 개수"만 측정 → 연결·정리보다 양산을 보상하고, "숙성도" 섹션(정리 권장)과도 자기모순. 앱 컨셉(연결)과 어긋남.
- **1단계**: 레벨/XP 카드 + "연결망 밀도" + "자주 쓰는 태그" 제거 → **라이브 연결 그래프 히어로 위젯**으로 교체(전역 그래프의 `computeForceLayout`/`colorForTag` 재사용). 노트/연결 수 + 태그 범례 + "전체 그래프 보기".
- **2단계**: "고정된 카드"·"아이디어 숙성도" 리스트 섹션 제거 → **그래프 노드 마커**로 흡수(고정=금색 테두리, 최근 수정<2일=펄스, 2주+ 미수정=흐림) + 마커 범례. "최근 카드" 행은 이름 클릭 내비게이션이라 유지.
- **3단계**: 활동 히트맵·월별 캘린더 색을 깃허브 초록 → **앱 테라코타(Claude) 팔레트**로 통일(라이트/다크). 콘텐츠 발견(캔버스·오늘의 한 장)을 활동 차트 **위로** 재배치.

### 히어로 그래프 다듬기
- 사이드 패널 오버플로우로 "전체 그래프 보기" 버튼이 **잘리던 버그** 수정(고정 높이 + 내부 스크롤 + 태그 범례 4개 제한).
- 마커 범례 색이 실제 노드와 어긋나던 문제 수정 — 범례 점을 색이 아니라 **상태 신호(펄스/흐림/테두리)** 로 표현.
- **노드 라벨 상시 표시**(길면 `…`, 가장자리 노드는 라벨 정렬을 안쪽으로 → 잘림 방지), 클러스터 자동 맞춤, **상위 16개(연결 많은 순) 제한 + "외 N개"** → 수백 개여도 안 뭉개짐.
- 1회 정적 레이아웃 → **살아있는 힘-시뮬레이션**(반발 + 링크 스프링 + 중심 중력 + 감쇠, d3식 **alpha 냉각**으로 자동 정지). **ResizeObserver**로 창/패널 크기 변화 시 재배치, 대시보드 벗어나면 정지.

### 실시간 동기화 — 에코 토스트 버그
- 본인 저장이 Supabase Realtime으로 되돌아와 "다른 기기에서의 변경을 반영했습니다" 토스트가 **타이핑을 멈출 때마다** 뜨던 문제. `sameNoteSig()`(updated_at 무시)로 에코를 감지해 조용히 반영 — 토스트·에디터 방해 없음. 진짜 원격 변경만 알림.

### 그래프 노드 드래그
- **히어로**: 노드를 잡으면 고정되고 시뮬레이션이 나머지를 물리적으로 반응시킴(옵시디언 느낌). 위치는 저장 안 됨(미리보기). "가장 가까운 노드" 히트 판정. 무이동 클릭=노트 열기.
- **전역 그래프**: 드래그 위치를 `localStorage: cardCatalogGraphPositions`에 저장 → **재오픈해도 배치 유지**. 모든 노드에 저장된 위치가 있으면 force 레이아웃을 다시 안 돌리고 그대로 사용(새 노트 생겼을 때만 재시뮬). 드래그 중 연결선 실시간 갱신.

### 참고
- `schema.sql` 변경 있음(캔버스 컬럼/테이블 DROP) — 실 DB에도 반영 완료.
- 검증: 헤드리스 브라우저(Playwright)로 드래그·클릭·위치 지속·에코 억제·리사이즈·라이트/다크를 시뮬레이션 확인. 커밋마다 스크립트 문법·DOM id·CSS 중괄호 정적 검증.

---

## 2026-07-26 작업 요약

이날 앱을 "마크다운 노트"에서 **4가지 작업 공간(일반·캔버스·테이블 + 데이터베이스 뷰)** 을 갖춘 도구로 확장하고, 뷰 관리 구조를 리팩터하고 다수 버그를 잡았다.

### 노트 타입 (content 필드 접두사로 구분 — DB 스키마 변경 없음)
- **일반(마크다운)** — 기존.
- **🎨 캔버스** (` ```canvas `, `isCanvasNote`) — FigJam풍 에디터. **저장을 JSON**(`{elements,connectors}`)으로 전환, 레거시 `@`-directive는 `parseCanvasLegacy()`로 폴백 로드.
- **📊 테이블** (` ```table `, `isTableNote`) — 독립 스프레드시트.
- **🗄 데이터베이스** — 노트 타입이 아니라 **전역 단일 뷰**(사이드바 검색 위 버튼). 설정은 `localStorage: stk-db`.

### 캔버스 에디터(prefix `cev`) 강화
- Undo/Redo(Ctrl+Z / Ctrl+Shift+Z, 60스텝), 복제(Ctrl+D), 전체선택(Ctrl+A).
- 다중 선택(마퀴 드래그 + Shift+클릭), 그룹 이동/삭제/복제/정렬.
- 격자 스냅(⊞), 전체 보기(⤢), 레이어 순서(맨앞/맨뒤), 잠금.
- 노드/그룹/커넥터별 **컨텍스트 툴바**(색·글자색·크기·굵기·복제·정렬·잠금·삭제, 커넥터는 종류·색·라벨).
- **섹션(⊡)** 을 옮기면 내부 노드와 화살표가 함께 이동.
- **카드 노드(📇)** — 실제 노트 카드를 캔버스에 얹고 더블클릭으로 열기(`edata.noteId`).
- 패닝: 가운데 버튼 드래그 / Space+드래그(좌드래그는 마퀴 선택).
- 버그 수정: `cevDragMoved` 미초기화로 **도형이 생성되다 안 되던 문제**, 섹션 미직렬화로 **저장 시 사라지던 데이터 유실**.

### 테이블 에디터(prefix `tev`)
- 열 타입 text/number/checkbox/date/select, 헤더 클릭 정렬, 하단 합계/평균/개수 요약.
- 엑셀/시트 붙여넣기(TSV), CSV 가져오기/내보내기, 행·열 우클릭 중간 삽입/삭제.
- 실시간 동기화 시 편집 중 경고, 로컬 `updated` 반영, number는 원문 문자열 저장(정렬/집계 때만 파싱).

### 데이터베이스 뷰(prefix `cov`)
- 태그를 고르면 그 태그 카드가 **행**, 각 카드 Properties가 **열**. 셀 인라인 편집 → **원본 카드에 양방향 반영**, 제목 클릭 시 카드 열기, "+ 새 항목"으로 태그 단 카드 생성.

### 아키텍처 / UX
- **뷰 관리 단일화**: 모든 mainpane 전환을 `showView(name)` + `VIEW_DEF` 레지스트리로 통합(각 뷰가 rightpane/toolbar 적용 여부 선언). 흩어진 display 조작으로 인한 "왔다갔다 버그" 제거.
- **좌측 사이드바 접기 기능 제거**(문제 잦아 항상 표시). 우측 패널만 토글 — 토글 버튼을 패널 **왼쪽 경계 세로 중앙 탭**으로 이동. 패널 폭 CSS 변수는 `:root`로 이동(고정 탭이 리사이즈 추적).
- 검색: 캔버스/테이블은 JSON 원문 대신 **실제 텍스트만 인덱싱**(`noteSearchText`, 캐시).
- 사이드바 카드 목록 **스크롤 수정**(`.card-tab { flex-shrink:0 }`, `.cardlist { min-height:0 }`), 태그줄 높이 제한.
- 브랜드: `Stacker`→`STACKER`→**GRAPHIDEA**, 부제 `note stack`→`idea stack`→**Idea Connect**.
  (표시 텍스트만 변경 — GitHub 저장소명/배포 URL/Supabase 프로젝트/localStorage 키는 그대로.)
- 온보딩 사용법 전면 재작성 + **1회 안내 모달**(`?` 버튼으로 재열람, `localStorage: stk-guide-v2`).
- 대시보드 월별 활동 **날짜 시간대 버그 수정**: UTC 대신 `localDateKey()`로 로컬 날짜 기준 집계.

### 참고
- 이날 **DB 스키마 변경 없음**. 새 기능은 모두 `notes.content` 또는 localStorage 사용.
- 저장 포맷: 캔버스/테이블/(레거시)컬렉션 노트는 각각 ` ```canvas ` / ` ```table ` / ` ```collection ` + JSON.
