# 기획안: 노드 선택 → 연결 관계 패널

> 상태: **구현 완료(2026-08-21)**. 전역 그래프(캔버스) 위 상호작용 기능.
> 관련 코드: `www/index.html`의 `renderGlobalGraph` / `drawGlobalGraph` / `graphCanvas` 이벤트 핸들러 + `selectGraphNode`/`renderGraphPanel`/`graphConnectionsOf`/`centerGraphOnNode`, `www/style.css`의 `.graph-conn-panel`/`.gcp-*`.
>
> **채택한 결정(8번 항목)**: ①카드 이동 = 더블클릭 + 패널 "카드 열기" 버튼 둘 다 / ②연결 항목 클릭 = 선택 이동(walk, 그래프에 있으면 중앙으로 팬·없으면 그 카드 열기) / ③패널 = 그래프 전용 오버레이 도킹(캔버스 리사이즈·재시뮬 없음), 모바일 하단 시트 / ④깨진 위키링크 = 흐리게(클릭 불가) 노출. 안드로이드 뒤로가기는 패널을 먼저 닫고 다음 back에서 그래프를 나감.

## 1. 목적 / UX 개념
현재는 노드를 클릭하면 **즉시 그래프를 닫고 카드로 이동**한다. 이걸 바꿔서:
- **1차 클릭 = "선택"** — 카드로 넘어가지 않고, 오른쪽에 그 노드가 **무엇과 어떻게 연결됐는지** 보여주는 패널을 연다.
- **카드로 이동은 명시적 행동**으로 분리(더블클릭 또는 패널의 "카드 열기" 버튼).
- 패널의 연결 항목을 클릭하면 그래프 선택이 그 노드로 **옮겨가며**(walk), 그래프를 손으로 안 찾아도 탐색할 수 있게 한다.

즉 그래프를 "바로 열기" 도구에서 → **"연결 탐색" 도구**로 승격한다.

## 2. 동작 변경
| 입력 | 현재 | 변경 후 |
|---|---|---|
| 노드 클릭/탭 | 그래프 닫고 카드 열기 | **선택** + 패널 열기 + 선택 하이라이트 |
| 빈 캔버스 클릭 | (없음) | 선택 해제 + 패널 닫기 |
| 노드 더블클릭 | (없음) | 카드 열기(기존 이동) |
| 패널 "카드 열기" 버튼 | — | 카드 열기 |
| 패널 연결 항목 클릭 | — | 그 노드로 선택 이동(패널·하이라이트 갱신) |

모바일은 더블탭 대신 패널의 "카드 열기" 버튼을 주 경로로 한다.

## 3. 패널 구성 (오른쪽 도킹)
```
┌─────────────────────────────┐
│ 📝 노드 제목            [✕]  │  ← 타입 아이콘 + 제목
│ #태그1 #태그2                │
│ 연결 8 · 나가는 5 · 백링크 3 │  ← 요약 통계
├─────────────────────────────┤
│ ▸ 나가는 링크 (5)            │  ← 이 노트가 [[링크]]한 카드
│   • 카드 A                   │
│   • 카드 B  (없는 링크는 흐리게)│
│ ▸ 들어오는 링크 / 백링크 (3) │  ← 이 노트를 링크한 카드
│   • 카드 C                   │
├─────────────────────────────┤
│        [ 카드 열기 → ]        │
└─────────────────────────────┘
```
- 각 연결 항목: 태그색 점 + 제목, 클릭 시 그래프 선택 이동.
- "나가는 링크" 중 대상이 실제로 없는 위키링크(깨진 링크)는 흐리게 표시(선택 이동 불가).

## 4. 데이터 — 방향성 있는 연결
현재 `graphAdjacency`는 **무방향** 이웃 집합이다. 패널은 방향을 구분해야 하므로 렌더 시 추가 계산:
- **나가는 링크**: `note.links`(제목 배열)를 소문자 매칭으로 실제 노드에 해석 → 존재/부재 표시.
- **백링크(들어오는)**: 다른 노트의 `links`에 이 제목이 들어있는 것들. `renderGlobalGraph`의 엣지 구축 루프가 이미 전 노트를 훑으므로, 그때 `outMap[id]=[]`, `inMap[id]=[]`를 같이 채우면 **추가 순회 없이** 확보 가능.
- 지연 로딩 주의: `links`는 목록 필드(`NOTE_LIST_FIELDS`)에 포함되어 본문 없이도 이미 로드됨 → 패널은 본문 요청 없이 즉시 구성 가능.

## 5. 그래프 ↔ 패널 연동
- 새 상태 `graphSelectedId`. 선택되면 `drawGlobalGraph`에서 **hover와 동일한 dim 로직을 지속 적용**(선택 노드 + 이웃만 밝게) + 선택 노드에 강조 링(accent).
- 호버는 일시적, 선택은 고정 — 선택 중 호버는 선택 위에 얹힌다.
- 패널 연결 클릭 → `graphSelectedId` 교체 + 해당 노드를 화면 중앙으로 **부드럽게 팬** 후 재선택.

## 6. 기존 코드 영향
- **클릭 핸들러**(`graphCanvas` click / touchend): "닫고 열기" → "선택"으로 교체. 더블클릭 리스너 추가. `graphNodeMoved`/`graphPanMoved` 가드 유지.
- **레이아웃**: `#globalGraphView`는 현재 `flex-column`(헤더·범례·캔버스). 캔버스와 패널을 나란히 두려면 캔버스+패널을 `flex-row`로 감싸는 래퍼 추가. 앱 공용 `#rightpane`(VIEW_DEF에서 graph는 `rightpane:false`)은 **재사용하지 않고** 그래프 전용 패널로 두는 게 결합도상 깔끔.
- **캔버스 리사이즈**: 패널이 열리면 캔버스 폭이 줄어든다. 이미 `ResizeObserver`가 `renderGlobalGraph`를 재호출해 자동 대응하지만, 재-시딩되며 레이아웃이 살짝 이완됨 — 원하면 패널 토글 시엔 재시딩 없이 base 변환(baseScale/baseTx/baseTy)만 갱신하는 경량 경로를 별도로 둔다.
- **모바일**: 오른쪽 도킹 대신 **하단 시트/슬라이드오버**. 안드로이드 뒤로가기(`handleBackNav`)에 "패널 열림이면 먼저 닫기" 단계 추가.

## 7. 단계적 구현 제안
- **Stage A**: `graphSelectedId` + 선택 하이라이트 + 클릭=선택 / 더블클릭=열기 (패널 없이 동작 전환만).
- **Stage B**: 오른쪽 패널 UI + 방향성 연결 데이터(나가는/백링크) 표시 + "카드 열기" 버튼.
- **Stage C**: 패널 연결 클릭으로 선택 이동 + 선택 노드 중앙 팬(walk).
- **Stage D**: 모바일 하단 시트 + 뒤로가기 연동.

## 8. 결정 필요 항목
1. **카드 이동 트리거**: 더블클릭 + 패널 버튼 둘 다? 아니면 패널 버튼만(모바일 일관성)?
2. **연결 항목 클릭 동작**: 선택 이동(walk) vs 곧바로 그 카드 열기 — 기본안은 walk.
3. **패널 위치**: 그래프 전용 오른쪽 도킹(기본안) vs 앱 공용 rightpane 재사용.
4. **깨진 위키링크**(대상 없음)도 "나가는 링크"에 흐리게 노출할지.
