# 이미지 저장 — 구글 드라이브 연동 계획 (보류, 참고용)

2026-08-06에 논의만 하고 보류한 아이디어. 나중에 이미지 삽입 기능을 만들 때 여기서부터 시작.

## 왜 구글 드라이브인가

개인용(지인/커뮤니티 소규모) 단계에서는 Supabase 무료 저장 용량(1GB)보다 구글 드라이브 개인 계정 기본 용량(15GB)이 훨씬 크고 비용도 안 든다. 이미지 바이너리는 사용자 본인의 구글 드라이브에 저장하고, Supabase DB(notes 테이블)에는 그 파일의 ID/링크 문자열만 저장 — DB 부담은 텍스트 수준으로 유지.

## 기본 구조

- 노트 content는 여전히 마크다운 텍스트. 이미지는 마크다운 이미지 문법(`![](url)`)으로 삽입, url이 드라이브 링크.
- 업로드: Drive REST API(`/upload/drive/v3/files`)에 멀티파트 업로드, 별도 플러그인 없이 fetch만으로 가능.
- 스코프: `drive.file` (앱이 만든 파일만 접근하는 좁은 권한). 개인용(테스트 계정 1개)으로 쓰는 동안은 구글의 OAuth verification 심사 불필요 — 나중에 여러 명한테 공개할 때만 필요.
- `drive.file`로 업로드한 파일은 사용자의 "내 드라이브" 최상위 폴더에 안 보이고 앱 전용 공간처럼 취급됨(앱을 통해서만 접근). 원하면 앱에서 특정 폴더를 만들어 넣어 눈에 보이게 할 수도 있음.
- 표시 방식 두 가지:
  - **파일 권한을 "링크 있는 사람 보기"로 설정 + 링크를 바로 `<img>`에 박기** — 구현 간단, 개인용이면 이걸로 충분
  - 매번 access token으로 인증된 요청 후 blob으로 렌더 — 더 안전하지만 토큰 만료·갱신 처리가 추가로 필요

## 놓치면 안 되는 기술적 함정: 액세스 토큰 만료

구글 로그인 시 받는 Drive 액세스 토큰은 **보통 1시간 뒤 만료**된다. 로그인 시 받은 토큰만 그대로 쓰면 1시간 지난 뒤 업로드가 실패함. 계속 쓰려면:

- OAuth 요청 시 `access_type=offline`으로 **refresh token**을 같이 받아야 함
- refresh token은 클라이언트에 노출하면 안 되는 값 → Supabase DB에 저장하고, **토큰 갱신만 담당하는 Edge Function**을 하나 둬서 클라이언트는 항상 이 함수를 호출해 유효한 access token만 받는 구조가 정석 (기존 `delete-account` Edge Function과 같은 패턴)

## DB 스키마 관련 판단

노트마다 `storage_provider`(`'drive'` | `'supabase'`) 같은 컬럼을 미리 만들어두자는 아이디어가 있었으나 **보류**. 이미지 URL 자체가 `drive.google.com`이냐 아니냐로 이미 출처가 구분되므로 별도 컬럼 없이도 나중에 유저별로 다른 방식을 섞을 수 있음. 진짜 여러 유저가 붙어서 필요해질 때 그때 결정.

## 실행 순서 (착수 시)

1. **(사용자 작업)** Google Cloud Console에서 OAuth 클라이언트 생성 + Drive API 활성화
2. **(사용자 작업)** Supabase Auth 대시보드에서 Google 프로바이더 켜고 client id/secret 등록 (offline access 포함)
3. **(코드)** 토큰 갱신용 Edge Function 작성 → 대시보드에서 배포
4. **(코드)** 앱에 구글 로그인 버튼 추가(기존 이메일 로그인은 유지, 추가 옵션) + 이미지 업로드 버튼 + 마크다운 이미지 렌더링(현재 전혀 없는 신규 기능) 구현
5. **(코드, 마지막)** 안드로이드 이식 — Capacitor 앱의 OAuth 리다이렉트(딥링크) 처리 필요, 아직 검토 안 함

## 현재 상태 (2026-08-06 기준)

- 구글 로그인 자체가 아직 코드에 없음 (이메일/비번 로그인만 존재)
- 마크다운 이미지 렌더링 자체가 아직 없음 (완전 신규 기능)
- 착수 전이므로 위 계획은 실제 구현 중 세부 사항이 바뀔 수 있음
