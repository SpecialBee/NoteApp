# GRAPHIDEA — 기능별 구현 기록

> 이 문서는 [`CHANGELOG.md`](CHANGELOG.md)(날짜순 작업 일지)와 반대로 **기능/영역별로** 정리한 문서입니다.
> 각 항목은 **무엇인지 → 왜 이렇게 만들었는지(의도) → 어떻게 구현했는지**를 다룹니다.
> 폴더를 복사해서 새 트랙(개인용/출시용)으로 나눌 때, 그 시점까지의 전체 맥락을 한 번에 파악하는 용도로 작성했습니다.

---

## 1. 프로젝트 정체성 · 기술 스택

- **이름**: GRAPHIDEA (부제 "Idea Connect"). 이전 명칭 STACKER → 2026-07-26 리브랜딩.
- **컨셉**: 위키링크(`[[제목]]`)로 카드를 서로 연결하고, 그 연결을 그래프로 시각화하는 노트 앱. 대시보드도 "카드를 얼마나 쌓았나"가 아니라 **"생각을 얼마나 연결했나"**를 보여주는 방향으로 설계(2026-07-27 재설계).
- **구조**: 단일 파일 웹앱(`www/index.html`, 스타일은 `www/style.css`로 분리) + 순수 함수 모음(`www/lib/constants.js`, `www/lib/utils.js`). 프레임워크 없음 — 바닐라 HTML/CSS/JS.
  - **왜 단일 파일인가**: 처음부터 그렇게 시작했고, 배포 파이프라인이 단순(정적 파일 서빙)해서 지금까지 유지. 2026-08-04에 딱 한 번 "유지보수 파일구조 분리 1단계"로 순수 함수·상수만 `lib/`로 뺐음 — 상태(전역 변수 `index`, `currentId`, `sb` 등)와 얽힌 로직(캔버스 에디터, 그래프, 대시보드, 실시간 동기화)은 억지로 쪼개면 회귀 위험이 커서 일부러 그대로 뒀음.
- **백엔드**: Supabase(Postgres + Auth + Realtime + Edge Functions). RLS(Row Level Security)로 사용자별 데이터 격리.
- **배포**: 웹은 Vercel 자동 배포(GitHub 연동, `vercel.json`의 `outputDirectory: "www"`). 앱은 Capacitor로 같은 `www/index.html`을 감싸 안드로이드 APK로 빌드 — **웹과 앱이 코드베이스를 공유**(따로 안 짬).
- **GitHub**: SpecialBee/NoteApp · 배포 URL: https://note-app-gamma-wheat.vercel.app

---

## 2. 데이터 모델 (`schema.sql`)

### `notes` 테이블
| 컬럼 | 의도 |
|---|---|
| `id`, `user_id`, `created_at`, `updated_at` | 기본. `updated_at`은 트리거로 자동 갱신. |
| `title`, `content`, `tags[]`, `links[]`, `pinned` | 노트 본체. `links`는 본문에서 `extractLinks()`로 뽑아낸 `[[제목]]` 목록(그래프/백링크의 기반). |
| `deleted_at` | 소프트 삭제(휴지통/복원). |
| `properties` (jsonb) | Notion 스타일 프로퍼티 배열(`{id,name,type,value,options?}`). 텍스트/숫자/날짜/선택/체크박스/**롤업** 지원. |
| `type` | `note`/`canvas`/`table`. 원래는 `content`가 ` ```canvas `/` ```table `로 시작하는지 클라이언트에서 매번 스니핑했는데, 목록·그래프·검색이 본문을 안 불러오고도 아이콘을 정할 수 있도록 실제 컬럼으로 승격(2026-08-04). |
| `content_len` (generated) | `length(content)`를 Postgres가 계산 — 대시보드 "총 글자 수"를 본문 전체를 안 불러오고도 합산하기 위해. |

**인덱스**: `(user_id, deleted_at, updated_at desc)` — RLS가 모든 쿼리에 암묵적으로 거는 `user_id = auth.uid()` 필터는 Postgres가 FK를 자동 인덱싱하지 않아 원래 풀스캔이었음. 앱의 실제 쿼리 모양(목록 로드)에 맞춰 복합 인덱스로 해결, `ORDER BY updated_at desc`까지 이 인덱스로 커버.

**RLS 정책**: select/insert/update/delete 4개 전부 `auth.uid() = user_id` — 이 앱의 데이터 격리는 전적으로 이 정책에 의존.

**`schema.sql` 파일 자체의 성격**: 실 운영 DB에 순서대로 실행해온 누적 기록이라, 한 번에 처음부터 끝까지 실행해도 에러 없이 통과하도록 컬럼 추가 → 그 컬럼을 쓰는 인덱스 순서로 재배치해둠(2026-08-06 수정 — 원래는 인덱스가 컬럼보다 앞에 있어서 빈 프로젝트에 처음 실행하면 실패했음).

### `checklist_items` 테이블
대시보드 "할 일" 위젯. 원래 `localStorage`에만 있던 유일한 사용자 콘텐츠였는데, 여러 기기 동기화가 안 되는 게 노트 앱 정체성과 안 맞아서 2026-07-28에 노트와 동일한 사용자별 RLS 패턴으로 클라우드 이전. 기존 로컬 항목은 첫 동기화 때 자동 마이그레이션.

### Realtime
`notes` 테이블에 `supabase_realtime` publication 추가 — 같은 계정으로 여러 탭/기기에서 열어두면 서로의 변경이 실시간 반영됨. 본인 저장이 되돌아오는 "에코"는 `sameNoteSig()`(updated_at 제외 비교)로 구분해 조용히 반영(2026-07-27, 원래는 타이핑 멈출 때마다 토스트가 떴음).

---

## 3. 노트 타입 4가지

콘텐츠는 전부 `notes.content` 한 필드에 저장하고, 타입에 따라 포맷이 다름(2026-07-26 도입, 2026-08-04부터 `type` 컬럼으로 명시).

### 3.1 일반(마크다운) 노트
가장 기본 타입. 구조는 위에서 아래로: `titlefield`(제목) → `titlerule`(구분선) → `tagchipwrap`(태그 칩 + 입력) → `fmttoolbar`(서식 도구상자) → `editor-split`(에디터/미리보기).

**서식 도구상자**: H1~H4, 굵게/기울임/취소선/하이라이트, 인용/링크/코드/표, 들여쓰기/내어쓰기, 할일/콜아웃/구분선. 굵게 등 토글형 버튼은 이미 적용된 선택 영역에 다시 누르면 해제됨(2026-07-29). 서식 적용은 `document.execCommand('insertText', …)`를 거쳐서 브라우저 네이티브 Ctrl+Z 스택이 안 끊김(2026-07-29 — 원래는 `textarea.value`를 스크립트로 직접 덮어써서 실행취소가 그 지점에서 끊겼음).

**위키링크 자동완성**: `[[`를 입력하면 커서 근처에 기존 카드 제목 드롭다운(캐럿 화면 좌표는 미러 `<div>` 기법으로 계산). ↑/↓/Enter/Tab/클릭으로 선택, 일치하는 카드 없으면 "새 카드로 연결됨" 안내(2026-07-29).

**콜아웃**: 💡 버튼 → 4종(📝노트·💡팁·⚠️주의·🚫위험) 팝오버에서 선택해 삽입(`openCalloutPicker`, `insertCallout(type)`) — 원래는 콜아웃 종류가 없었는데 캔버스 컨텍스트 툴바와 비슷한 형태로 확장(2026-08-05).

**읽기모드 & 비교**: 미리보기 전용 화면. `⧉ 비교` 버튼으로 현재 카드|피커로 고른 다른 카드를 좌우로 나란히 볼 수 있음(가운데 드래그 구분선, 각 창 독립 스크롤). **데스크탑/태블릿 전용**, 모바일(≤640px)은 버튼 자체를 숨김(2026-08-05).

**미리보기 동기화**: 분할(split) 모드에서 편집 중 커서가 문서 전체에서 차지하는 위치 비율만큼 미리보기도 그 비율 지점으로 스크롤(설정에서 끄고 켤 수 있음, 기본 켜짐). 이 방식에 도달하기까지 한 세션 동안 3번 갈아엎었음: ①"편집할 때마다 맨 밑으로" → 서식 버튼 클릭에도 반응하는 버그 → ②"진짜 타이핑(`isTrusted`)만 반응" → 그래도 맨 밑 근처에서 작업 중이면 여전히 튐 → ③"최하단 추적" 개념 자체를 버리고 커서 비율 동기화로 교체(2026-07-29). **교훈으로 메모리에 남긴 것**: 스크롤/포커스 관련 기능은 여러 편집 위치(맨 위/중간/맨 밑)에서 직접 확인하고 넘길 것.

**본문 지연 로딩**: 노트 목록·그래프·검색·위키링크 자동완성은 `NOTE_LIST_FIELDS`(가벼운 필드만)로 불러오고, 본문(`content`)은 카드를 열 때 `ensureContentLoaded(note)`로 개별 요청(2026-08-04, Play 스토어 "무제한 페이지네이션" 이슈 대응). **알려진 트레이드오프**: 세션 중 한 번도 안 연 노트의 본문은 검색에 안 걸림(제목·태그 검색은 영향 없음) — 서버 쪽 페이지네이션 대신 이 방향을 택한 건 사용 패턴상(개인/소규모 노트 앱) 본문 전체 서버 검색보다 구현 단순성이 더 급했기 때문.

### 3.2 캔버스 카드 (FigJam 스타일, prefix `cev`)
` ```canvas ` + JSON(`{elements, connectors}`)으로 저장. 도형(스티키/사각형/원/다이아몬드/텍스트/섹션/카드노드) + 커넥터(화살표/실선/점선/점선화살표).

**핵심 기능**: Undo/Redo(60스텝), 다중선택(마퀴+Shift클릭), 그룹 이동/삭제/복제/정렬, 격자 스냅, 전체보기, 레이어 순서, 잠금, 도형별 컨텍스트 툴바(색·글자색·크기·굵기 등), 섹션(내부 노드가 같이 이동), 카드노드(실제 노트를 캔버스에 얹고 더블클릭으로 열기).

**리사이즈 핸들과 연결 앵커의 충돌 (2026-08-06 근본 수정)**: 원(circle)의 크기 조절이 전혀 안 되던 버그의 실제 원인은 계산식이 아니라 — 연결용 앵커(`.cev-anchor`)가 상하좌우 리사이즈 핸들과 **완전히 같은 좌표**에 더 높은 z-index로 얹혀 있었고 `pointer-events`가 안 보일 때도 항상 켜져 있어서 핸들 클릭을 계속 가로채고 있던 것. 해결: 원에도 사각형처럼 모서리 포함 8방향 핸들(모서리는 앵커와 안 겹침) + 앵커 `pointer-events`를 보일 때만 켜지도록 + 데스크탑은 도형 선택 시 핸들이 앵커보다 우선(모바일의 "선택 시 앵커로 연결" 동작은 유지). **이 세션에서 얻은 교훈**: 겹쳐 보이는 UI 버그는 계산 로직보다 z-index/pointer-events 충돌을 먼저 의심할 것.
- 같은 원리로 **마름모 드래그 생성 시 정사각 미보장** 버그도 있었음(리사이즈 핸들 쪽만 w==h를 강제하고 생성 로직엔 없었음) — 생성 로직에도 동일 제약 추가.

**두 손가락 팬/줌**: 핀치줌 중 손가락을 같은 방향으로 이동해도 화면이 고정돼 있던 버그 — 제스처 시작 시 손가락 중점 아래 캔버스 좌표를 저장해두고 매 프레임 그 좌표가 현재 중점 아래 유지되도록 팬을 재계산(2026-08-06).

**모바일 선연결**: 근본 원인 2건 — ① 앵커 점에 `touchstart` 자체가 없어서 터치로는 연결 시작이 불가능했음. ② 터치엔 `:hover`가 없어 `.cev-el:hover > .cev-anchor`가 영원히 안 떴음 → 선택된 도형에 앵커를 표시하는 방식으로 전환, coarse 포인터에서 22px로 확대(2026-08-05).

**컨텍스트 툴바 잔류 버그**: 도형 선택 후 다른 카드로 이동해도 그 도형의 툴바가 화면에 남아있던 문제. 최초 수정은 `openCanvasEditor()` 안에만 있어서 캔버스→캔버스 전환에만 적용됐고, 캔버스→일반노트/시트 전환은 그 함수를 안 타서 안 고쳐졌음 — 모든 카드 전환의 공통 진입점인 `openNote()`로 옮겨 근본 해결(2026-08-06).

**태그 편집**: 원래 캔버스/시트 카드엔 태그 편집 수단이 아예 없었음(2026-07-28에 우측 패널에 처음 추가). 2026-08-06에 상단 헤더에도 태그 칩 UI를 추가(우측 패널의 `rpAddTag`/`rpRemoveTag`를 그대로 재사용해 자동 동기화).

### 3.3 시트 카드 (스프레드시트, prefix `tev`)
` ```table ` + JSON(`{columns, rows}`)으로 저장.

**열 타입**: text/number/checkbox/date/select + **link(연결)**/**formula(계산)**(2026-08-06 신설).
- **select**: 옵션에 태그 색상(`colorForTag`) 재사용, 선택된 셀 자체도 채색.
- **link**: 셀이 다른 카드를 참조. 캔버스 카드 피커(`cevOpenCardPicker`)와 동일한 패턴의 검색형 피커(`tevOpenLinkPicker`)로 연결/변경, 클릭 시 해당 카드로 이동, 별도 ✎ 버튼으로 재연결.
- **formula**: 같은 행의 다른 숫자 열들을 합계/평균/개수로 계산(`tevComputeFormula`) — **저장 안 하고 매번 재계산되는 파생 값**(그래프 기반 롤업과 같은 설계 철학).
- 타입 변경은 클릭식 순환에서 드롭다운 선택으로 전환(2026-08-06).

**필터**: 열 값 기준 행 필터(텍스트 포함/체크박스/연결여부/선택옵션), 필터링된 행 기준으로 하단 집계(합/평균/개수)도 재계산(2026-08-06). 정렬(`tevSortCol`)과 마찬가지로 뷰 상태일 뿐 저장 안 함.

**모바일 대응**: 행/열 중간 삽입·열 삭제가 우클릭 전용이고 삭제 버튼도 hover 전용이라 모바일에서 안 보이고 안 됐음 — 롱프레스(`tevBindLongPress`)로 컨텍스트 메뉴 호출, 터치에서 삭제 버튼 상시 노출로 수정(2026-08-06).

**기타**: 헤더 클릭 정렬, 엑셀/시트 붙여넣기(TSV), CSV 가져오기/내보내기(연결 열은 카드 제목으로, 계산 열은 계산값으로 내보냄), 파괴적 작업(열타입변경·열삭제·행삭제) 전부 확인창.

### 3.4 데이터베이스 뷰 (전역 뷰, prefix `dbView`, 노트 타입 아님)
태그를 고르면 그 태그가 달린 카드가 행, Properties가 열인 스프레드시트 형태 뷰. 셀 인라인 편집이 원본 카드에 양방향 반영. "노트 타입"이 아니라 사이드바 검색 위 버튼으로 진입하는 **전역 화면**이라는 점이 시트 카드와 다름 — 2026-07-27에 이름이 겹쳐서(당시 "데이터 카드"였던 시트 카드와) "데이터 카드"→"시트 카드"로 개명하며 정리.

---

## 4. 그래프 & 연결 시스템

### 그래프 기반 롤업 프로퍼티 (2026-08-04)
Notion의 relation+rollup을 별도 relation 필드 없이 구현 — 이미 있는 위키링크 그래프를 그대로 relation처럼 사용(`connectedNotesOf`: 나가는 링크 + 들어오는 백링크 전부). 프로퍼티 타입에 "Σ 롤업" 추가, 연결된 카드 중 지정한 이름의 숫자 프로퍼티를 합계/평균/개수로 집계(`computeRollup`). **순환 참조 설계**: 롤업은 다른 롤업 프로퍼티를 읽지 않도록 만들어서 A→B→A 루프가 애초에 불가능.

### 연결 지도 / 대시보드 히어로 그래프
둘 다 같은 계열의 **alpha-냉각 힘-시뮬레이션**(반발+링크스프링+중심중력+감쇠, d3 스타일)을 쓰고, 멈춘 뒤에도 완전 정지하지 않고 노드당 2~4px·7~12초 주기의 느린 사인파로 계속 표류(`startIdleFloat`, 2026-08-04 — 그래프가 "죽어있어 보인다"는 인상을 줄이기 위한 의도적 연출). 설정에서 켜고 끌 수 있음(2026-08-06, 저사양 기기 배터리 절약용).

> **명칭(2026-08-21)**: 원래 "전역 그래프"로 부르던 화면이 **"연결 지도"**로 통일됐다. 그 이름은 우측 패널의 작은 미니 그래프가 쓰고 있었는데, 노드 연결관계 패널이 생기면서 미니 그래프가 하던 역할(이 카드의 이웃 보기)을 연결 지도가 더 잘 하게 되어 **미니 그래프를 삭제하고 이름을 넘겨받았다**. 코드 내부 식별자(`globalGraph*`, `renderGlobalGraph`, `drawGlobalGraph` 등)는 그대로 두었다 — 전부 개명하면 히스토리 추적만 어려워지고 얻는 게 없어서.

- **연결 지도**: 드래그 위치를 `localStorage`에 저장해 재오픈해도 유지, 모든 노드에 저장된 위치가 있으면 재시뮬 안 함(2026-07-27). 유사-3D 깊이 효과(토글 가능) — 배포 직후 해시 함수가 음수를 낼 수 있어 그래프가 통째로 사라지는 회귀가 있었고, 부호 없는 정수로 강제해 수정(2026-07-28).
  - **SVG → Canvas 전환 (2026-08-20, 렉 근본 해결)**: 노드가 많아질수록 남던 렉의 실제 병목은 계산이 아니라 렌더링이었음 — `step()`이 매 프레임 노드마다 `setAttribute('transform')`, 선마다 좌표 4회씩 SVG 속성을 갱신해(노드 500·선 800이면 초당 약 22만 회) retained SVG 씬의 스타일 재계산·레이아웃을 반복 무효화하고 있었다. **물리 시뮬레이션은 그대로 두고 그리기 계층만** `<canvas>` 즉시 모드 1회 repaint(`drawGlobalGraph`)로 교체해 근본 해결(Obsidian이 그래프에 Canvas/WebGL을 쓰는 것과 같은 이유). 부속 변경: DPI 대응 백버퍼, CSS변수→rgb 해석(캔버스는 `var(--…)`를 직접 못 씀), DOM 대신 수동 최근접 히트테스트, world→canvas "meet" 베이스 변환으로 기존 viewBox 팬/줌 감각 재현.
    - **팬/줌 좌표 함정**: `graphView.tx/ty`가 viewBox 단위인데 팬/줌 계산을 화면 px로 해서 `baseScale`(노드 많을수록 <1)만큼 어긋났음 — 끌어서 이동이 짧고 휠 확대가 좌상단으로 쏠림. 팬은 `/baseScale`, 줌은 커서를 viewBox 좌표로 환산해 고정점으로 쓰도록 수정.
    - **라벨은 화면 좌표에서 별도 2-패스로** 렌더(월드 변환에 안 딸려 고정 12px로 선명), 배경색 후광(`strokeText`)으로 엣지 위 가독성 확보, 우선순위 정렬 후 겹침 사각형 컬링으로 밀집 구역 글자 뭉침 방지.
    - **입체(depth) 모드**는 캔버스에서 per-node 블러가 비싸므로 **대기 원근**으로 전환 — 먼 노드일수록 색을 배경색 쪽으로 바래고(`mix(fill,bg,(1-z)*0.62)`) 작게, 노드 빌드 시 사전 보간해 캐시(매 프레임 비용 0). 단순 투명도보다 깊이로 읽힘.
    - **교훈**: "고쳤는데도 남는 성능 문제"는 계산(알고리즘)만 보지 말고 **렌더 계층의 DOM 조작 비용**을 의심할 것. retained-mode(SVG/DOM)에서 매 프레임 대량 속성 갱신은 immediate-mode(canvas)로 갈아타는 게 정석.
  - **노드 연결관계 패널 (2026-08-21)**: 노드를 클릭하면 바로 카드로 이동하던 걸 **클릭=선택+오른쪽 연결 패널 / 더블클릭·버튼=카드 열기**로 분리해, 그래프를 "바로 열기"에서 "연결 탐색" 도구로 승격. 패널은 그 노드의 나가는 링크/백링크를 방향 구분해 목록화(`graphConnectionsOf` — `links`가 목록 필드라 본문 로드 불필요, 대상 없는 위키링크는 흐리게), 항목 클릭 시 선택이 그 노드로 옮겨가며 중앙으로 팬(walk, `centerGraphOnNode`). **패널은 오버레이 도킹**(모바일 하단 시트)이라 토글해도 캔버스 리사이즈/재시뮬이 없음. `drawGlobalGraph`는 hover와 selection을 합친 focus로 dim/라벨을 처리. 결정 이력·상세는 `CONNECTION-PANEL-PLAN.md`.
- **대시보드 히어로 그래프**: 레벨/XP 시스템("카드 개수"만 측정해 연결·정리보다 양산을 보상하던 것)을 걷어내고 이 그래프로 교체(2026-07-27 대시보드 재설계 1~3단계). 고정=금색 테두리, 최근 수정<2일=펄스, 2주+ 미수정=흐림 마커. 여전히 SVG(노드가 16개로 캡돼 있어 렉이 없음).
- ~~**우측 패널 미니 그래프**~~: 현재 카드를 중앙에 고정하고 이웃을 반발+스프링으로 정착시키던 240×150 SVG 그래프. **2026-08-21 삭제** — 연결 지도의 노드 연결관계 패널이 같은 질문("이 카드는 뭐랑 이어져 있지?")에 훨씬 잘 답하게 되면서 역할이 겹쳤고, 우측 패널에서 자리만 차지하고 있었음. 우측 패널은 이제 속성 / 역링크 / 캔버스 보드 / 태그 / 목차로 구성.

### 검색
캔버스/테이블은 JSON 원문이 아니라 실제 텍스트만 인덱싱(`noteSearchText`, 캐시). 본문 지연 로딩 도입 이후엔 이미 본문을 불러온(연 적 있는) 노트만 본문 검색 대상 — 제목/태그는 항상 검색됨.

---

## 5. 인증 · 계정 · 보안

### RLS 검증
실제 계정 2개를 만들어 교차 검증(2026-08-04): 기존 노트가 새 계정엔 전혀 안 보이는 것, 새 노트가 그 계정에만 귀속되는 것, 탈퇴 후 재로그인 실패까지 실측. **주의사항으로 남길 것**: 같은 브라우저의 여러 탭은 로그인 세션을 공유하므로, 같은 오리진에서 테스트 계정으로 로그인하면 실제 계정 세션이 잠깐 덮어써질 수 있음(그 세션엔 발생했으나 데이터 손실 없이 재로그인으로 복구).

### 회원 탈퇴
anon key로는 auth 계정을 못 지우므로(관리자 권한 필요) Supabase Edge Function(`supabase/functions/delete-account`)을 통해 처리 — 호출자 JWT를 검증한 뒤 service-role로 본인 계정만 삭제, 노트·체크리스트는 `auth.users` FK의 `on delete cascade`로 자동 정리. **CORS 이슈**: 배포 직후 curl로는 되는데 브라우저에서 "Failed to fetch"가 났음 — curl은 CORS를 무시하지만 브라우저는 아니라서, 프리플라이트(OPTIONS) 처리와 CORS 헤더 추가로 해결. Edge Function은 CLI 없이 Supabase 대시보드 에디터에 직접 붙여넣어 배포하는 방식으로 관리.

### 비밀번호
- **재설정 딥링크**: `redirectTo: window.location.href`가 앱 안에서는 `file://`/`capacitor://` 주소라 재설정 이메일의 브라우저가 못 열었음 — 고정된 배포 URL(`SITE_URL`)로 교체.
- **로그인 상태에서 바로 변경**(2026-08-06): 재설정 이메일 플로우 없이 설정 화면에서 즉시 변경 가능.

### XSS
- 2026-07-30: 대시보드 "오늘의 한 장" 미리보기가 노트 본문을 `escapeHtml` 없이 `innerHTML`에 꽂던 저장형 XSS 수정, 전체 파일 재점검.
- 2026-08-06: `escapeHtml`이 `"`/`'`를 이스케이프하지 않아 위키링크 제목·외부 링크 URL로 `data-link`/`href` 속성을 탈출해 삽입할 수 있었던 것 추가 발견·수정.

### 개인정보처리방침 (`www/privacy.html`)
2026-08-04 신설, 2026-08-06에 국외이전 조항(이전 항목/받는 자 Supabase Inc./국가 싱가포르 AWS `ap-southeast-1`/시기·방법/목적·보유기간/거부 시 효과) 및 보유기간 항목(DB는 즉시 삭제되지만 인프라 제공업체 백업·로그엔 그쪽 주기대로 남을 수 있다는 현실적 단서) 보완.

---

## 6. 데이터 안정성

### 백그라운드 저장 flush
앱이 백그라운드로 전환될 때(`visibilitychange`, Capacitor `App` pause/appStateChange) 대기 중이던 디바운스 저장을 강제 flush — 모바일에서 앱 전환/OS 강제종료로 방금 쓴 내용이 날아가는 걸 방지(2026-07-30 도입).
- **2026-08-06 근본 수정**: 이 핸들러들이 일반 노트의 `flushSave()`만 부르고 캔버스(`cevFlushSave`)·시트(`tevFlushSave`)는 안 불러서, **뒤로가기가 아닌 홈 버튼/앱 전환으로 나가면** 편집 중이던 캔버스·시트 내용이 유실될 수 있었음(뒤로가기 경로엔 이미 셋 다 부르고 있어서 안 걸렸던 것) → `flushAllSaves()`로 통합.

### 전체 백업
설정 → 데이터에서 모든 카드 + 할 일을 JSON 파일 하나로 다운로드(2026-08-04). 이전엔 노트 1개씩 md 내보내기만 있었음.

### 자동저장 유실 (2026-07-27)
캔버스 편집 중 저장 debounce(600ms) 전에 다른 카드로 이동하면 마지막 편집이 유실되던 문제 — `openNote()`/`exitCanvasEditor()`에 `cevFlushSave()` 추가(시트 에디터엔 이미 있던 패턴을 캔버스에도 적용).

### 첫 로드 버벅임 (2026-08-21)
"앱을 열 때 한 번에 많은 정보를 열어 버벅인다"는 체감을 부팅 경로에서 추적해 **중복 작업**을 제거. 대부분 계산이 아니라 "같은 일을 두 번 하거나 O(n²)로 하던" 문제였음:
- **`renderCardList` O(n²) → O(n)**: `buildCardEl`이 카드마다 `index.findIndex`(문서번호용)를 돌던 것을 `id→위치` Map 1회로 교체.
- **DocumentFragment**: 라이브 `#cardList`에 카드를 N번 append하며 리플로우가 반복되던 것을 1회 삽입으로.
- **부팅 병렬화**: 불필요한 `getUser()` 왕복 제거(세션의 user 사용), `loadAll`·`loadChecklist`를 `Promise.all`로 병렬.
- **카드 리스트 이중 렌더 제거**: `boot()`에서 1회 + 이어지는 `openDashboard()`에서 또 1회 렌더하던 것 정리.
- **⭐ boot-once 가드**: `onAuthStateChange`가 `getSession()`과 겹쳐 초기 로드에 `boot()`이 두 번 돌고, **토큰 갱신(TOKEN_REFRESHED)·USER_UPDATED마다 전체 재로딩**되던 것을 `bootedUserId`로 user별 1회만 부팅하도록 방지(로그아웃 시 리셋). 실시간 동기화는 유지돼 데이터 최신성엔 영향 없음. **교훈**: 세션을 실어 나르는 auth 이벤트는 로그인 외에도(초기·토큰갱신·유저갱신) 여러 번 발화하므로, 무거운 초기화는 반드시 1회 가드로 감쌀 것.

---

## 7. 모바일 · 안드로이드 대응

### 원칙
2026-07-28에 "앞으로 모든 작업은 모바일 버전도 함께 확인"을 1원칙으로 확정 — 그 전까지 모바일에서 사실상 못 쓰는 수준이었던 걸 근본 원인부터 고치고 나서 세운 원칙.

### 반응형 레이아웃 근본 원인 (2026-07-28)
`applyPanelState()`가 화면 전환마다 `gridTemplateColumns`를 **인라인 스타일**로 강제 설정 → 인라인 스타일은 항상 CSS/미디어쿼리보다 우선이라, 이미 작성돼 있던 반응형 규칙이 한 번도 실제로 적용된 적이 없었음. 좁은 화면에서도 사이드바가 항상 280px를 차지해 나머지가 쥐어짜이던 원인. `window.innerWidth`를 직접 읽어 분기하도록 수정. **이 세션에서 반복적으로 재발견한 패턴**: CSS 우선순위/소스 순서 버그(인라인 vs 클래스, 나중 규칙이 이기는 동점 상황)가 "고쳤는데 안 고쳐진 것처럼 보이는" 버그의 단골 원인 — 2026-08-06 모바일 헤더 축소 작업에서도 똑같은 유형의 버그(모바일 규칙이 파일 앞쪽에 있어서 뒤쪽 데스크탑 규칙에 덮임)가 재발했음.

### Android 15 edge-to-edge (2026-07-30)
API 35+ 기본 edge-to-edge 렌더링으로 앱 콘텐츠가 상태표시줄 영역까지 그려지는데 그 영역 터치는 시스템이 가로채서, 최상단 버튼(햄버거 등)이 터치가 안 먹던 버그. `viewport-fit=cover` + 모든 최상단 바에 `env(safe-area-inset-top)` 여백.
- **2026-08-06 관련 버그**: 캔버스/시트 편집화면은 유일하게 `.mobile-topbar`와 자체 상단바(`.cev-topbar`/`.tev-topbar`)가 두 줄 겹치는 화면인데, 둘 다 각자 `safe-area-inset-top`을 더하고 있어서 노치 여백이 이중으로 들어가고 있었음 — 모바일 전용 규칙에서는 자체 상단바 쪽 안전영역 패딩을 제거.

### 터치 입력
마우스 전용이던 전역 그래프(팬/드래그/휠줌), 대시보드 히어로 그래프(드래그), 캔버스 에디터(이동·리사이즈·마퀴선택·생성·팬·줌) 전부 터치 대응(2026-07-28) — 핸들러를 이름 있는 함수로 리팩터해서 마우스와 터치가 같은 코드 경로를 타도록 함. 두 손가락 핀치줌+팬은 마우스에 없던 제스처라 신규 작성.

### Android 뒤로가기 (2026-08-05)
Capacitor `App` `backButton` 리스너 + `handleBackNav()` — 팝오버 → 모달 → 모바일 오버레이(우측패널·사이드바) → 열린 뷰(편집분 flush 후 홈+목록) 순으로 한 단계씩 해제, 더 없으면 `minimizeApp()`(종료 아니라 백그라운드로).

### 헤더 크기 문제 (2026-08-06, 여러 라운드)
캔버스/시트 편집화면과 일반 노트 카드 모두 겪은 문제: **버튼 줄바꿈이 높이를 잡아먹는 진짜 원인**이었음(패딩이 아니라). 각 도구모음(캔버스 도구상자·상단바, 시트 상단바, 노트 카드 `.fmttoolbar` 17개 버튼)을 전부 한 줄 고정 + 가로 스크롤로 전환. 노트 카드 상단 `.toolbar`는 폭에 따라 자동 줄바꿈되며 순서가 들쭉날쭉했던 걸, 명시적으로 두 줄(`.toolbar-row1`=모드버튼, `.toolbar-row2`=나머지 전부)로 분리 — 데스크탑에서는 두 그룹 div가 `display:contents`로 사라져서 기존과 동일하게 한 줄로 보임. 모바일에서 문서번호·마지막 수정일자·읽는시간은 숨기고 글자 수만 표시. 실기기 스크린샷으로 헤더 높이가 화면의 약 38% → 18%까지 줄어든 것 확인.

### 세 에디터 헤더 구조 통일 (2026-08-06)
노트 카드는 원래부터 "제목 → 구분선 → 태그 → 도구상자" 구조였는데, 캔버스/시트는 모든 버튼이 한 줄에 뒤섞여 있었음. 노트 카드와 동일한 구조로 재배치하고, 상단바(액션 버튼)와 도구상자(편집 도구)의 역할을 명확히 분리(노트 카드의 `.toolbar` 대응 위치에 캔버스는 실행취소/스냅/보기/줌/색상/삭제/나가기, 시트는 가져오기/내보내기/나가기 배치. 도구상자엔 캔버스는 도형·연결선 타입만, 시트는 필터만).

---

## 8. 설정 화면

2026-07-29에 처음 생겼고(테마 색상·글꼴·마크다운 하이라이트 색·미리보기 동기화 토글), 2026-08-04에 데이터(백업)·계정(약관·탈퇴) 섹션 추가, **2026-08-06에 화면/편집/성능/데이터/계정 5개 카테고리로 재구성**하며 신규 항목 추가:
- 다크모드 토글(사이드바 버튼과 값 공유)
- 캔버스 격자 스냅 기본값(캔버스를 열 때마다 적용 — 이전엔 세션 내내 하나의 값이 새고 있었음)
- 그래프 유휴 애니메이션 on/off
- 휴지통 자동 비우기(7/30/90일, 휴지통을 열 때 기준일 지난 카드를 영구 삭제)
- 로그인 상태에서 바로 쓰는 비밀번호 변경
- 다크모드에서 네이티브 `<select>` 드롭다운이 흰 배경으로 뜨던 문제 수정(`color-scheme` CSS 속성 지정 — 브라우저가 폼 컨트롤을 다크로 그리게 하는 표준 힌트).

---

## 9. 배포 · 인프라

### Capacitor 도입 (2026-07-30)
개인적으로 계속 쓰다가 추후 Play 스토어 정식 출시를 목표로 시작. `index.html`/`icon.png`를 저장소 루트 → `www/`로 이동(히스토리 보존), `package.json`/`capacitor.config.json`(appId `com.specialbee.graphidea`, **정식 출시 전 확정 필요** — Play 스토어 게시 후 변경 불가)/`vercel.json` 추가, `npx cap add android`로 `android/` 생성 후 저장소에 커밋(Capacitor 표준 관행).

### Supabase SDK 번들 내장 (2026-08-04)
jsdelivr CDN의 버전 미고정(`@2`)이 언제든 breaking change를 몰래 들여올 수 있어서, `npm install @supabase/supabase-js`로 정확한 버전을 고정해 `www/vendor/supabase.js`로 번들. 버전 올릴 땐 `npm run vendor:supabase`.

### 안드로이드 빌드 명령
```
export JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"
npx cap sync android
cd android && ./gradlew.bat assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am start -n com.specialbee.graphidea/.MainActivity
```
자세한 환경 세팅(SDK 경로, 드라이버 등)은 `CONTINUE-ON-ANOTHER-COMPUTER.md` 참고.

---

## 10. 보류 중인 계획

### 구글 드라이브 이미지 저장 (`IMAGE-STORAGE-PLAN.md`, 2026-08-06 논의만 하고 보류 → 2026-08-11 구현 완료)
개인용 단계에서는 Supabase 무료 저장 용량(1GB)보다 구글 드라이브 개인 계정(15GB)이 크고 무료라서 나온 아이디어. 이미지 바이너리는 사용자 드라이브에, Supabase엔 파일 링크만 저장. **놓치면 안 되는 함정으로 기록해둔 것**: 구글 액세스 토큰은 보통 1시간 뒤 만료되므로 `access_type=offline`으로 refresh token을 받아 Edge Function에서 갱신하는 구조가 필요(그냥 로그인 시 토큰만 쓰면 1시간 뒤부터 업로드 실패). `storage_provider` 같은 DB 컬럼은 이미지 URL 자체(`drive.google.com` 포함 여부)로 출처를 구분할 수 있어 불필요하다고 판단, 만들지 않기로 함. (구현 상세는 `CHANGELOG.md` 2026-08-11 참고.)

### 노드 선택 연결관계 패널 (`CONNECTION-PANEL-PLAN.md`, 2026-08-20 기획 → 2026-08-21 구현 완료)
전역 그래프에서 노드를 클릭하면 곧바로 카드로 이동하던 걸, **클릭=선택+연결 패널 / 더블클릭=카드 열기**로 분리해 그래프를 탐색 도구로 만든 기획. 패널은 그 노드의 **나가는 링크와 백링크를 방향 구분**해 보여주고, 연결 항목을 클릭하면 선택이 그 노드로 옮겨가며 그래프를 walk(그래프에 있으면 중앙으로 팬, 없으면 그 카드를 엶). `links`가 이미 목록 필드라 본문 로드 없이 구성 가능. 기획 당시 결정 대기였던 항목은 모두 확정: 카드 이동=더블클릭+패널 버튼 / 연결 클릭=walk / 패널=그래프 전용 오버레이 도킹(모바일은 하단 시트) / 깨진 위키링크=흐리게 노출. (구현 상세는 `CHANGELOG.md` 2026-08-21 참고.)

---

## 11. 알려진 트레이드오프 / 아직 안 한 것

- **본문 지연 로딩의 검색 공백**: 세션 중 한 번도 안 연 노트는 본문 검색에 안 걸림(제목·태그는 영향 없음). 서버 쪽 전체 본문 검색으로 바꾸려면 별도 작업 필요.
- **다중 기기 동시 편집**: 충돌 시 경고 토스트만 뜨고 실제 저장은 마지막 쓰기가 이김(revision 컬럼 등 실제 충돌 방지 로직 없음).
- ~~**1,000행 이상 노트**: Supabase/PostgREST 기본 응답 상한(1,000행)에 페이지네이션 없이 걸려있음~~ → **2026-08-21 해결**. 노트가 실제로 1,000개에 근접하면서 현실 문제가 됨. PostgREST는 상한에서 잘라내면서 **오류를 내지 않기 때문에** 목록·검색·그래프에서 노트가 조용히 사라지고, 특히 **백업 파일이 소리 없이 불완전해지는** 게 위험했음. `fetchAllRows()` 헬퍼(1,000행씩 `.range()` 순회)를 만들어 전체 노트/휴지통/JSON 백업/마크다운 zip 내보내기/할 일 목록 등 테이블 전체를 반환할 수 있는 모든 쿼리에 적용.
- **자동화 테스트 없음**: 검증은 매번 문법 파싱(`new Function()`) + 실기기/에뮬레이터 스크린샷 확인으로 대체해옴. CI/회귀 테스트 스위트는 없음.
- **Play 스토어 정식 출시 전 남은 것**: `appId` 확정, 릴리스 키스토어, 스토어 등록정보(스크린샷/설명), Play Console 개발자 계정, Google Play 정책상 필요한 **웹에서도 되는 계정 삭제 페이지**(현재는 앱 내부에서만 가능), Android `targetSdk` 최신화. 전부 "실제 제출 시점에 처리"로 미뤄둔 상태.
- **iOS**: 플랫폼 범위 자체가 아직 미정.
